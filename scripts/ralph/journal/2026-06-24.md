# Ralph Journal — 2026-06-24

### 18:22 UTC · CHORE-002 — Deploy workflows use drizzle-kit push (not db:migrate) + document in DEPLOY.md
- **type:** chore (P0, area ci)
- **gate:** green (change confined to .github/workflows/*.yml + docs/DEPLOY.md — outside lint/typecheck/test scope; both workflows re-validated as parseable YAML, job-ids intact so `needs:` stay valid; `ci` not deploy-relevant → no eval)
- **decision:** open-pr (mode pr-only)
- **action:** Switched `staging.yml` (job `db-migrate`) and `production.yml` (job `db-migrate-prod`) from `pnpm run db:migrate` to `pnpm run db:push`; removed the now-meaningless journal-driven `db:migrate:dry-run` step in production; added explanatory comments (schema = single source of truth, non-forced push so destructive ops abort the job, first prod push must be done manually per DEPLOY.md §2/§7). Updated `docs/DEPLOY.md` §2/§5/§7 to mark the switch done and explain why `db:push` is the authority. Updated memoria §7 checkbox, §8 status, §10 changelog.
- **commit:** chore: [CHORE-002] - deploy workflows use drizzle-kit push, document in DEPLOY.md
- **pr:** opened to main after push
- **notes:** Workflows kept job-ids `db-migrate`/`db-migrate-prod` to avoid touching downstream `needs:` refs. Residual `db:migrate` strings are only in explanatory comments. Pre-existing unrelated working-tree change `apps/web/src/lib/dev-port.test.ts` was deliberately NOT included in this atomic commit. Still gated on a non-prod DB: the push must be validated on staging before the prod merge.
