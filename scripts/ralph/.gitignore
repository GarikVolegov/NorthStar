# Kill-switch is a local control surface — never commit it.
STOP
# Per-run shell logs (the markdown journal IS committed; these raw logs are not).
journal/run-*.log
# Loop-local secrets (DATABASE_URL, etc.) — sourced by loop.sh, NEVER committed.
.env.loop
