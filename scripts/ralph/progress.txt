# Ralph Progress Log
Started: 2026-06-17 (seeded by Opus 4.8 — sub-progetto A "stabilize-current-line")
---

## Codebase Patterns
- READ FIRST each iteration: memoria.md (root) + the relevant *_RULES.md for the area you touch (API_RULES.md, DB_RULES.md, FRONTEND_RULES.md, AI_RULES.md). memoria.md §10 changelog has the latest state.
- BRANCH: work on `ralph/stabilize-current-line`, ALREADY created from release/launch-candidate (which holds Stripe/monetization/CI + 62 commits ahead of main). Do NOT branch from main — it lacks all the recent work.
- GATE (pnpm is NOT on PATH here): `export PATH="$HOME/.local/bin:$PATH"` first. Typecheck server: `cd apps/server && node --max-old-space-size=4096 ../../node_modules/typescript/bin/tsc --noEmit`. Typecheck web: `cd apps/web && node --max-old-space-size=4096 ../../node_modules/typescript/bin/tsc -p tsconfig.json --noEmit`. Lint: from repo root `./node_modules/.bin/eslint <files> --max-warnings=0` (do NOT prefix with `node`). Tests: `apps/server/node_modules/.bin/vitest run <file> --configLoader runner --root apps/server`; web tests `apps/web/node_modules/.bin/vitest run <file> --configLoader runner --root apps/web`.
- ROUTES: web pages mounted in apps/web/src/route-config.ts (guard 'public'|'publicOnly'|'protected', layout 'default'|'admin'|'plain', lazy import). Server routers in apps/server/src/route-config.ts (auth 'public'|'authenticated'|'admin'). A router mounted 'public' can still protect sub-paths with an internal guard.
- AUTH: apps/web/src/components/ProtectedRoute.tsx is Clerk-based; redirects unauthenticated to /sign-in, PublicOnlyRoute redirects signed-in to /dashboard. lib/clerk-config.ts isClerkConfigured() gates behavior when VITE_CLERK_PUBLISHABLE_KEY is unset.
- SECURITY: userId ALWAYS from req.user.id (never the body) — prevents IDOR. No secrets committed. Only the user's OWN data may go to the LLM (no third-party PII).
- DB: Drizzle schema in packages/db/src/schema/<name>.ts (some tables live inside users.ts — grep before adding). Tests use a REAL DB (no mock) per DB_RULES.md — needs DATABASE_URL/Postgres+pgvector; if absent, write the test and guard with `describe.skipIf(!process.env.DATABASE_URL)` + note it runs in CI/staging. Journal drift known (idx 34 vs 44 SQL files, memoria §8) — use hand-written idempotent RAW SQL migrations, do NOT run drizzle-kit generate.
- DEPLOY: ainorthstar.vercel.app = appsweb Vercel project, framework vite. Root vercel.json buildCommand currently runs the WHOLE quality gate before building (fragile). CI (.github/workflows/ci.yml) is the place for the gate, not the Vercel build.
---

## SEEDED DIAGNOSIS (Opus 4.8, before run) — read before picking a story

### US-001 (login) — ROOT CAUSE FOUND
- ProtectedRoute.tsx redirects to /sign-in (lines 38, 51) but apps/web/src/route-config.ts registers NO /sign-in, /sign-up or /login route (only /registra, /register, /reset-password). Pages apps/web/src/pages/sign-in.tsx + sign-up.tsx EXIST but are orphaned → any login attempt hits NotFound.
- Fix = wire those pages into route-config with guard 'publicOnly'. Pure frontend, no backend. Mirror the existing /registra + /register entries.

### US-002 (news) — LIKELY DATA, NOT CODE
- apps/server/src/routes/news.ts GET path is DB-backed: SELECT from newsArticlesTable filtered by PUBLIC_NEWS_SOURCES (no live fetch in GET). If news_articles is empty on the local/fresh DB, /news is blank. Prod has it populated → looks fine there.
- Reproduce FIRST (confirm the table is empty / whether an ingestion job exists in apps/server/src/jobs). Then either run/seed ingestion OR add a static fallback so /news is never blank.

### US-003 (deploy) — ARCHITECTURAL FIX
- Last 4 GitHub deploys are ERROR; production frozen on a 2026-06-02 CLI deploy of feature/fase2-cervello-runtime (commit 9c9a450). Build log of the latest ERROR (9958a96): lint+typecheck+ai/server/web tests PASS; log truncates inside the audit steps → exact failing audit not pinned. Fix = drop `pnpm run quality:required && ` from the ROOT vercel.json buildCommand (keep server build + web build). Final deploy verification is founder-side (push → watch Vercel go READY).
---

## 2026-06-17 - US-001 (login) — DONE
- The seeded hypothesis (orphaned /sign-in route) was WRONG: App.tsx:145-172 already routes /sign-in + /sign-up via a wouter Switch before delegating to route-config. Reading App.tsx before coding avoided shipping a useless route registration.
- REAL root cause: frontend auth is Clerk-only; the publishable key never reached the Vite bundle. It lived in the repo-root .env as NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY (Next-style), but Vite (root=apps/web, default envDir) only exposes VITE_* vars and reads apps/web/.env. So isClerkConfigured()=false everywhere → login impossible.
- Fix (founder chose: keep Clerk, expose the key — fast path): apps/web/vite.config.ts now loadEnv(mode, repoRoot, "") and injects import.meta.env.VITE_CLERK_PUBLISHABLE_KEY via `define`, accepting VITE_ or legacy NEXT_PUBLIC_ name, falling back to process.env for Vercel. clerk-config.ts reads the literal import.meta.env.VITE_CLERK_PUBLISHABLE_KEY (mirror sentry.ts) so define applies. .env.example documents it.
- Verified: web typecheck OK; eslint clerk-config.ts OK (vite.config.ts is outside the gate's lint scope → "File ignored", not a real warning); clean `vite build` OK and grep of dist/public/assets confirms pk_test_ is injected → end-to-end proof the key reaches the client.
- **Learnings for future iterations:**
  - There are TWO routers: App.tsx Switch (explicit escape-hatch routes incl. Clerk /sign-in, /sign-up, /admin, /workspace, /grafo redirects) wraps a catch-all that renders MainLayout + RouterFromConfig(route-config.ts). Check App.tsx BEFORE assuming a path is missing from route-config.
  - Vite client env: tsconfig types include vite/client so import.meta.env.X is `any`; inject non-VITE_ values via `define` of "import.meta.env.VITE_X" (pattern already used for VITE_SENTRY_RELEASE) and READ the literal expression (define is text replacement — a `const env = import.meta.env; env.X` alias is NOT replaced).
  - FOUNDER TODO (not code): set VITE_CLERK_PUBLISHABLE_KEY (or NEXT_PUBLIC_) in Vercel env, else prod login stays broken.
  - Bonus for US-003: standalone `vite build` (web) succeeds in ~3.5s — the web build itself is fine; only the quality:required wrapper breaks the Vercel deploy.
---

## 2026-06-17 - US-003 (deploy) — DONE
- Removed `pnpm run quality:required && ` from the ROOT vercel.json buildCommand. It now runs only the two real artifact builds: `pnpm --filter @northstar/server exec node build.mjs && pnpm --filter @northstar/web run build` (the Vercel deploy serves the API as serverless functions via the /api rewrite, so the server build must stay). outputDirectory / rewrites / CSP headers untouched.
- Verified locally: vercel.json is valid JSON; SERVER build → "✅ Build complete: api/index.js" (exit 0); WEB build → green (confirmed in US-001). So the full Vercel buildCommand succeeds.
- The full quality gate (lint + typecheck + 3 coverage suites + 9 audits) stays enforced in CI (.github/workflows/ci.yml), where Postgres+pgvector are provisioned — not in the Vercel sandbox which has no DB. JSON can't hold a comment, so this rationale lives in the commit + here.
- **Learnings for future iterations:**
  - Running quality gates inside the Vercel buildCommand is fragile: any gate step (ratchet/knip/db-types/test infra) aborts the production deploy. Keep CI and deploy concerns separate.
  - FOUNDER TODO (not code): (1) push so a GitHub deploy of release/launch-candidate goes READY; (2) ainorthstar.vercel.app still points to the old 2026-06-02 CLI deploy (target:production) — decide in Vercel whether to set the production branch to release/launch-candidate or promote a current deploy; (3) set VITE_CLERK_PUBLISHABLE_KEY in Vercel env (US-001) or prod login stays broken.
---

## 2026-06-17 - US-002 (news) — DONE (founder chose: static fallback)
- Root cause confirmed by code analysis: GET /api/news (apps/server/src/routes/news.ts) reads from newsArticlesTable, which is populated ONLY by the AI discovery agent (packages/ai-server/src/discovery-agent/news-publisher.ts, insert ... onConflictDoNothing). No simple seed/job. On a fresh/local DB the table is empty → blank page. Prod has it populated, hence it looks fine there.
- Fix: new apps/server/src/lib/news-fallback.ts — 6 evergreen NorthStar-authored items (clearly source "NorthStar", links point to real app routes, NOT fabricated external news) + pure functions getStaticNewsItems({category?,sector?,limit?}) / getStaticNewsDetail(id) / isStaticNewsId(id). news.ts returns the static set (source:'static') when the DB result is empty on the first page AND in the error catch, across all three routes (/, /sector/:name, and /article/:id for sample-* ids). Also: never read/write an empty cache entry (guards a stale empty cache from blanking the page).
- Verified: server typecheck OK, eslint OK, 8 unit tests green (pure functions → no DB mock, respects DB_RULES). Browser verification PENDING (needs app+DB running; do it locally/staging).
- **Learnings for future iterations:**
  - news_articles is filled by the discovery agent, not a seed. To get REAL news locally/staging you must run that agent (needs an LLM provider). The static fallback only fills the gap so /news is never blank.
  - The response shape is { news, nextCursor } (+ now an optional source:'live'|'static'); the frontend reads data.news. Static detail items use string ids 'sample-N' and the detail route now short-circuits on isStaticNewsId before parseInt.
  - news.tsx card click uses wouter navigate(detailUrl) (internal) so detailUrl must be an app path; the "Fonte" link uses sourceUrl as an external <a target=_blank>.
---
