---
description: Show the autonomous loop status — mode, kill-switch, backlog counts, and the next item.
---

Run `node scripts/ralph/ralph-cli.mjs status` and report the result. If the user asks, also
show the latest journal entry from `scripts/ralph/journal/` and the current rollout `mode` from
`scripts/ralph/loop.config.json`.
