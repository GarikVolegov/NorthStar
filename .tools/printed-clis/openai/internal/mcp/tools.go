// Copyright 2026 garik-v. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"openai-pp-cli/internal/cli"
	"openai-pp-cli/internal/client"
	"openai-pp-cli/internal/cliutil"
	"openai-pp-cli/internal/config"
	"openai-pp-cli/internal/mcp/cobratree"
	"openai-pp-cli/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("assistants_create",
			mcplib.WithDescription("Create an assistant with a model and instructions. Required: model. Optional: description, instructions, metadata (plus 7 more). Returns the new AssistantObject."),
			mcplib.WithString("description", mcplib.Description("Description")),
			mcplib.WithString("instructions", mcplib.Description("Instructions")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API...")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("reasoning_effort", mcplib.Description("Reasoning effort")),
			mcplib.WithString("response_format", mcplib.Description("Response format")),
			mcplib.WithString("temperature", mcplib.Description("Temperature")),
			mcplib.WithString("tool_resources", mcplib.Description("Tool resources")),
			mcplib.WithString("tools", mcplib.Description("A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types...")),
			mcplib.WithString("top_p", mcplib.Description("Top p")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/assistants", []mcpParamBinding{{PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "reasoning_effort", WireName: "reasoning_effort", Location: "body"}, {PublicName: "response_format", WireName: "response_format", Location: "body"}, {PublicName: "temperature", WireName: "temperature", Location: "body"}, {PublicName: "tool_resources", WireName: "tool_resources", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "top_p", WireName: "top_p", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("assistants_delete",
			mcplib.WithDescription("Delete an assistant. Required: assistant_id. Returns the DeleteAssistantResponse. Destructive."),
			mcplib.WithString("assistant_id", mcplib.Required(), mcplib.Description("The ID of the assistant to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/assistants/{assistant_id}", []mcpParamBinding{{PublicName: "assistant_id", WireName: "assistant_id", Location: "path"}}, []string{"assistant_id"}),
	)
	s.AddTool(
		mcplib.NewTool("assistants_get",
			mcplib.WithDescription("Retrieves an assistant. Required: assistant_id. Returns the AssistantObject."),
			mcplib.WithString("assistant_id", mcplib.Required(), mcplib.Description("The ID of the assistant to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/assistants/{assistant_id}", []mcpParamBinding{{PublicName: "assistant_id", WireName: "assistant_id", Location: "path"}}, []string{"assistant_id"}),
	)
	s.AddTool(
		mcplib.NewTool("assistants_list",
			mcplib.WithDescription("Returns a list of assistants. Optional: limit (default: 20), order (default: desc), after (plus 1 more)."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/assistants", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("assistants_modify",
			mcplib.WithDescription("Modifies an assistant. Required: assistant_id. Optional: description, instructions, metadata (plus 8 more). Returns the new AssistantObject."),
			mcplib.WithString("assistant_id", mcplib.Required(), mcplib.Description("The ID of the assistant to modify.")),
			mcplib.WithString("description", mcplib.Description("Description")),
			mcplib.WithString("instructions", mcplib.Description("Instructions")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("model", mcplib.Description("ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API...")),
			mcplib.WithString("name", mcplib.Description("Name")),
			mcplib.WithString("reasoning_effort", mcplib.Description("Reasoning effort")),
			mcplib.WithString("response_format", mcplib.Description("Response format")),
			mcplib.WithString("temperature", mcplib.Description("Temperature")),
			mcplib.WithString("tool_resources", mcplib.Description("Tool resources")),
			mcplib.WithString("tools", mcplib.Description("A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types...")),
			mcplib.WithString("top_p", mcplib.Description("Top p")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/assistants/{assistant_id}", []mcpParamBinding{{PublicName: "assistant_id", WireName: "assistant_id", Location: "path"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "reasoning_effort", WireName: "reasoning_effort", Location: "body"}, {PublicName: "response_format", WireName: "response_format", Location: "body"}, {PublicName: "temperature", WireName: "temperature", Location: "body"}, {PublicName: "tool_resources", WireName: "tool_resources", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "top_p", WireName: "top_p", Location: "body"}}, []string{"assistant_id"}),
	)
	s.AddTool(
		mcplib.NewTool("audio_create-speech",
			mcplib.WithDescription("Generates audio from the input text. Returns the audio file content, or a stream of audio events. Required: input, model, voice. Optional: instructions, response_format (default: mp3), speed (default: 1) (plus 1 more)."),
			mcplib.WithString("input", mcplib.Required(), mcplib.Description("The text to generate audio for. The maximum length is 4096 characters.")),
			mcplib.WithString("instructions", mcplib.Description("Control the voice of your generated audio with additional instructions. Does not work with `tts-1` or `tts-1-hd`.")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("One of the available [TTS models](/docs/models#tts): `tts-1`, `tts-1-hd`, `gpt-4o-mini-tts`, or...")),
			mcplib.WithString("response_format", mcplib.Description("The format to audio in. Supported formats are `mp3`, `opus`, `aac`, `flac`, `wav`, and `pcm`.")),
			mcplib.WithNumber("speed", mcplib.Description("The speed of the generated audio. Select a value from `0.25` to `4.0`. `1.0` is the default.")),
			mcplib.WithString("stream_format", mcplib.Description("The format to stream the audio in. Supported formats are `sse` and `audio`. `sse` is not supported for `tts-1` or...")),
			mcplib.WithString("voice", mcplib.Required(), mcplib.Description("The voice to use when generating the audio. Supported built-in voices are `alloy`, `ash`, `ballad`, `coral`, `echo`,...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/audio/speech", []mcpParamBinding{{PublicName: "input", WireName: "input", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "response_format", WireName: "response_format", Location: "body"}, {PublicName: "speed", WireName: "speed", Location: "body"}, {PublicName: "stream_format", WireName: "stream_format", Location: "body"}, {PublicName: "voice", WireName: "voice", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("audio_create-transcription",
			mcplib.WithDescription("Transcribes audio into the input language. Returns a transcription object in `json`, `diarized_json`, or `verbose_json` format, or a stream of transcript events. Required: file, model. Optional: chunking_strategy, include, known_speaker_names (plus 7 more)."),
			mcplib.WithString("chunking_strategy", mcplib.Description("Chunking strategy")),
			mcplib.WithString("file", mcplib.Required(), mcplib.Description("The audio file object (not file name) to transcribe, in one of these formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg,...")),
			mcplib.WithString("include", mcplib.Description("Additional information to include in the transcription response. `logprobs` will return the log probabilities of the...")),
			mcplib.WithString("known_speaker_names", mcplib.Description("Optional list of speaker names that correspond to the audio samples provided in `known_speaker_references[]`. Each...")),
			mcplib.WithString("known_speaker_references", mcplib.Description("Optional list of audio samples (as [data URLs](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/Data_...")),
			mcplib.WithString("language", mcplib.Description("The language of the input audio. Supplying the input language in...")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("ID of the model to use. The options are `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`,...")),
			mcplib.WithString("prompt", mcplib.Description("An optional text to guide the model's style or continue a previous audio segment. The...")),
			mcplib.WithString("response_format", mcplib.Description("The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, `vtt`, or `diarized_json`....")),
			mcplib.WithString("stream", mcplib.Description("Stream")),
			mcplib.WithNumber("temperature", mcplib.Description("The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower...")),
			mcplib.WithString("timestamp_granularities", mcplib.Description("The timestamp granularities to populate for this transcription. `response_format` must be set `verbose_json` to use...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/audio/transcriptions", []mcpParamBinding{{PublicName: "chunking_strategy", WireName: "chunking_strategy", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "file", WireName: "file", Location: "body", Format: "binary", RequestContentType: "multipart/form-data"}, {PublicName: "include", WireName: "include", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "known_speaker_names", WireName: "known_speaker_names", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "known_speaker_references", WireName: "known_speaker_references", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "language", WireName: "language", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "model", WireName: "model", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "prompt", WireName: "prompt", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "response_format", WireName: "response_format", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "stream", WireName: "stream", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "temperature", WireName: "temperature", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "timestamp_granularities", WireName: "timestamp_granularities", Location: "body", RequestContentType: "multipart/form-data"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("audio_create-translation",
			mcplib.WithDescription("Translates audio into English. Required: file, model. Optional: prompt, response_format (default: json), temperature (default: 0)."),
			mcplib.WithString("file", mcplib.Required(), mcplib.Description("The audio file object (not file name) translate, in one of these formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav,...")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("ID of the model to use. Only `whisper-1` (which is powered by our open source Whisper V2 model) is currently available.")),
			mcplib.WithString("prompt", mcplib.Description("An optional text to guide the model's style or continue a previous audio segment. The...")),
			mcplib.WithString("response_format", mcplib.Description("The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, or `vtt`.")),
			mcplib.WithNumber("temperature", mcplib.Description("The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/audio/translations", []mcpParamBinding{{PublicName: "file", WireName: "file", Location: "body", Format: "binary", RequestContentType: "multipart/form-data"}, {PublicName: "model", WireName: "model", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "prompt", WireName: "prompt", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "response_format", WireName: "response_format", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "temperature", WireName: "temperature", Location: "body", RequestContentType: "multipart/form-data"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("audio_create-voice",
			mcplib.WithDescription("Create a custom voice you can use for audio output (for example, in Text-to-Speech and the Realtime API). This requires an audio sample and a previously uploaded consent recording. See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers. Required: audio_sample, consent, name. Returns the new VoiceResource."),
			mcplib.WithString("audio_sample", mcplib.Required(), mcplib.Description("The sample audio recording file. Maximum size is 10 MiB. Supported MIME types: `audio/mpeg`, `audio/wav`,...")),
			mcplib.WithString("consent", mcplib.Required(), mcplib.Description("The consent recording ID (for example, `cons_1234`).")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The name of the new voice.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/audio/voices", []mcpParamBinding{{PublicName: "audio_sample", WireName: "audio_sample", Location: "body", Format: "binary", RequestContentType: "multipart/form-data"}, {PublicName: "consent", WireName: "consent", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "name", WireName: "name", Location: "body", RequestContentType: "multipart/form-data"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("audio_create-voice-consent",
			mcplib.WithDescription("Upload a consent recording that authorizes creation of a custom voice. See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers. Required: language, name, recording. Returns the new VoiceConsentResource."),
			mcplib.WithString("language", mcplib.Required(), mcplib.Description("The BCP 47 language tag for the consent phrase (for example, `en-US`).")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The label to use for this consent recording.")),
			mcplib.WithString("recording", mcplib.Required(), mcplib.Description("The consent audio recording file. Maximum size is 10 MiB. Supported MIME types: `audio/mpeg`, `audio/wav`,...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/audio/voice_consents", []mcpParamBinding{{PublicName: "language", WireName: "language", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "name", WireName: "name", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "recording", WireName: "recording", Location: "body", Format: "binary", RequestContentType: "multipart/form-data"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("audio_delete-voice-consent",
			mcplib.WithDescription("Delete a consent recording that was uploaded for creating custom voices. See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers. Required: consent_id. Returns the VoiceConsentDeletedResource. Destructive."),
			mcplib.WithString("consent_id", mcplib.Required(), mcplib.Description("The ID of the consent recording to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/audio/voice_consents/{consent_id}", []mcpParamBinding{{PublicName: "consent_id", WireName: "consent_id", Location: "path"}}, []string{"consent_id"}),
	)
	s.AddTool(
		mcplib.NewTool("audio_get-voice-consent",
			mcplib.WithDescription("Retrieve consent recording metadata used for creating custom voices. See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers. Required: consent_id. Returns the VoiceConsentResource."),
			mcplib.WithString("consent_id", mcplib.Required(), mcplib.Description("The ID of the consent recording to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/audio/voice_consents/{consent_id}", []mcpParamBinding{{PublicName: "consent_id", WireName: "consent_id", Location: "path"}}, []string{"consent_id"}),
	)
	s.AddTool(
		mcplib.NewTool("audio_list-voice-consents",
			mcplib.WithDescription("List consent recordings available to your organization for creating custom voices. See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers. Optional: after, limit (default: 20). Returns array of VoiceConsentResource."),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/audio/voice_consents", []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("audio_update-voice-consent",
			mcplib.WithDescription("Update consent recording metadata used for creating custom voices. This endpoint updates metadata only and does not replace the underlying audio. See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers. Required: consent_id, name. Returns the new VoiceConsentResource."),
			mcplib.WithString("consent_id", mcplib.Required(), mcplib.Description("The ID of the consent recording to update.")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The updated label for this consent recording.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/audio/voice_consents/{consent_id}", []mcpParamBinding{{PublicName: "consent_id", WireName: "consent_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"consent_id"}),
	)
	s.AddTool(
		mcplib.NewTool("batches_create-batch",
			mcplib.WithDescription("Creates and executes a batch from an uploaded file of requests. Required: completion_window, endpoint, input_file_id. Optional: metadata, output_expires_after. Returns the new Batch."),
			mcplib.WithString("completion_window", mcplib.Required(), mcplib.Description("The time frame within which the batch should be processed. Currently only `24h` is supported.")),
			mcplib.WithString("endpoint", mcplib.Required(), mcplib.Description("The endpoint to be used for all requests in the batch. Currently `/v1/responses`, `/v1/chat/completions`,...")),
			mcplib.WithString("input_file_id", mcplib.Required(), mcplib.Description("The ID of an uploaded file that contains requests for the new batch. See [upload...")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("output_expires_after", mcplib.Description("The expiration policy for the output and/or error file that are generated for a batch.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/batches", []mcpParamBinding{{PublicName: "completion_window", WireName: "completion_window", Location: "body"}, {PublicName: "endpoint", WireName: "endpoint", Location: "body"}, {PublicName: "input_file_id", WireName: "input_file_id", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "output_expires_after", WireName: "output_expires_after", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("batches_list",
			mcplib.WithDescription("List your organization's batches. Optional: after, limit (default: 20). Returns array of Batch."),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/batches", []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("batches_retrieve-batch",
			mcplib.WithDescription("Retrieves a batch. Required: batch_id. Returns the Batch."),
			mcplib.WithString("batch_id", mcplib.Required(), mcplib.Description("The ID of the batch to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/batches/{batch_id}", []mcpParamBinding{{PublicName: "batch_id", WireName: "batch_id", Location: "path"}}, []string{"batch_id"}),
	)
	s.AddTool(
		mcplib.NewTool("batches_cancel_batch",
			mcplib.WithDescription("Cancels an in-progress batch. The batch will be in status `cancelling` for up to 10 minutes, before changing to `cancelled`, where it will have partial results (if any) available in the output file. Required: batch_id. Returns the new Batch."),
			mcplib.WithString("batch_id", mcplib.Required(), mcplib.Description("The ID of the batch to cancel.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/batches/{batch_id}/cancel", []mcpParamBinding{{PublicName: "batch_id", WireName: "batch_id", Location: "path"}}, []string{"batch_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chat_create-completion",
			mcplib.WithDescription("**Starting a new project?** We recommend trying [Responses](/docs/api-reference/responses) to take advantage of the latest OpenAI platform features. Compare [Chat Completions with Responses](/docs/guides/responses-vs-chat-completions?api-mode=responses). --- Creates a model response for the given chat conversation. Learn more in the [text generation](/docs/guides/text-generation), [vision](/docs/guides/vision), and [audio](/docs/guides/audio) guides. Parameter support can differ depending on the model used to generate the response, particularly for newer reasoning models. Parameters that are only supported for reasoning models are noted below. For the current state of unsupported parameters in reasoning models, [refer to the reasoning guide](/docs/guides/reasoning). Returns a chat completion object, or a streamed sequence of chat completion chunk objects if the request is streamed. Required: messages, model. Optional: audio, frequency_penalty (default: 0), function_call (plus 30 more)."),
			mcplib.WithString("audio", mcplib.Description("Parameters for audio output. Required when audio output is requested with `modalities: ['audio']`. [Learn...")),
			mcplib.WithNumber("frequency_penalty", mcplib.Description("Number between -2.0 and 2.0. Positive values penalize new tokens based on their existing frequency in the text so...")),
			mcplib.WithString("function_call", mcplib.Description("Deprecated in favor of `tool_choice`. Controls which (if any) function is called by the model. `none` means the...")),
			mcplib.WithString("functions", mcplib.Description("Deprecated in favor of `tools`. A list of functions the model may generate JSON inputs for.")),
			mcplib.WithString("logit_bias", mcplib.Description("Modify the likelihood of specified tokens appearing in the completion. Accepts a JSON object that maps tokens...")),
			mcplib.WithBoolean("logprobs", mcplib.Description("Whether to return log probabilities of the output tokens or not. If true, returns the log probabilities of each...")),
			mcplib.WithNumber("max_completion_tokens", mcplib.Description("An upper bound for the number of tokens that can be generated for a completion, including visible output tokens and...")),
			mcplib.WithNumber("max_tokens", mcplib.Description("The maximum number of [tokens](/tokenizer) that can be generated in the chat completion. This value can be used to...")),
			mcplib.WithString("messages", mcplib.Required(), mcplib.Description("A list of messages comprising the conversation so far. Depending on the [model](/docs/models) you use, different...")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("modalities", mcplib.Description("Modalities")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI offers a wide range of models with different...")),
			mcplib.WithNumber("n", mcplib.Description("How many chat completion choices to generate for each input message. Note that you will be charged based on the...")),
			mcplib.WithBoolean("parallel_tool_calls", mcplib.Description("Whether to enable [parallel function...")),
			mcplib.WithString("prediction", mcplib.Description("Configuration for a [Predicted Output](/docs/guides/predicted-outputs), which can greatly improve response times...")),
			mcplib.WithNumber("presence_penalty", mcplib.Description("Number between -2.0 and 2.0. Positive values penalize new tokens based on whether they appear in the text so far,...")),
			mcplib.WithString("prompt_cache_key", mcplib.Description("Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field....")),
			mcplib.WithString("prompt_cache_retention", mcplib.Description("Prompt cache retention")),
			mcplib.WithString("reasoning_effort", mcplib.Description("Reasoning effort")),
			mcplib.WithString("response_format", mcplib.Description("An object specifying the format that the model must output. Setting to `{ 'type': 'json_schema', 'json_schema':...")),
			mcplib.WithString("safety_identifier", mcplib.Description("A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies. The...")),
			mcplib.WithNumber("seed", mcplib.Description("This feature is in Beta. If specified, our system will make a best effort to sample deterministically, such that...")),
			mcplib.WithString("service_tier", mcplib.Description("Service tier")),
			mcplib.WithString("stop", mcplib.Description("Not supported with latest reasoning models `o3` and `o4-mini`. Up to 4 sequences where the API will stop generating...")),
			mcplib.WithBoolean("store", mcplib.Description("Whether or not to store the output of this chat completion request for use in our [model...")),
			mcplib.WithBoolean("stream", mcplib.Description("If set to true, the model response data will be streamed to the client as it is generated using [server-sent...")),
			mcplib.WithString("stream_options", mcplib.Description("Stream options")),
			mcplib.WithString("temperature", mcplib.Description("Temperature")),
			mcplib.WithString("tool_choice", mcplib.Description("Controls which (if any) tool is called by the model. `none` means the model will not call any tool and instead...")),
			mcplib.WithString("tools", mcplib.Description("A list of tools the model may call. You can provide either [custom...")),
			mcplib.WithNumber("top_logprobs", mcplib.Description("An integer between 0 and 20 specifying the maximum number of most likely tokens to return at each token position,...")),
			mcplib.WithString("top_p", mcplib.Description("Top p")),
			mcplib.WithString("user", mcplib.Description("This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to...")),
			mcplib.WithString("verbosity", mcplib.Description("Verbosity")),
			mcplib.WithString("web_search_options", mcplib.Description("This tool searches the web for relevant results to use in a response. Learn more about the [web search...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/chat/completions", []mcpParamBinding{{PublicName: "audio", WireName: "audio", Location: "body"}, {PublicName: "frequency_penalty", WireName: "frequency_penalty", Location: "body"}, {PublicName: "function_call", WireName: "function_call", Location: "body"}, {PublicName: "functions", WireName: "functions", Location: "body"}, {PublicName: "logit_bias", WireName: "logit_bias", Location: "body"}, {PublicName: "logprobs", WireName: "logprobs", Location: "body"}, {PublicName: "max_completion_tokens", WireName: "max_completion_tokens", Location: "body"}, {PublicName: "max_tokens", WireName: "max_tokens", Location: "body"}, {PublicName: "messages", WireName: "messages", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "modalities", WireName: "modalities", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "n", WireName: "n", Location: "body"}, {PublicName: "parallel_tool_calls", WireName: "parallel_tool_calls", Location: "body"}, {PublicName: "prediction", WireName: "prediction", Location: "body"}, {PublicName: "presence_penalty", WireName: "presence_penalty", Location: "body"}, {PublicName: "prompt_cache_key", WireName: "prompt_cache_key", Location: "body"}, {PublicName: "prompt_cache_retention", WireName: "prompt_cache_retention", Location: "body"}, {PublicName: "reasoning_effort", WireName: "reasoning_effort", Location: "body"}, {PublicName: "response_format", WireName: "response_format", Location: "body"}, {PublicName: "safety_identifier", WireName: "safety_identifier", Location: "body"}, {PublicName: "seed", WireName: "seed", Location: "body"}, {PublicName: "service_tier", WireName: "service_tier", Location: "body"}, {PublicName: "stop", WireName: "stop", Location: "body"}, {PublicName: "store", WireName: "store", Location: "body"}, {PublicName: "stream", WireName: "stream", Location: "body"}, {PublicName: "stream_options", WireName: "stream_options", Location: "body"}, {PublicName: "temperature", WireName: "temperature", Location: "body"}, {PublicName: "tool_choice", WireName: "tool_choice", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "top_logprobs", WireName: "top_logprobs", Location: "body"}, {PublicName: "top_p", WireName: "top_p", Location: "body"}, {PublicName: "user", WireName: "user", Location: "body"}, {PublicName: "verbosity", WireName: "verbosity", Location: "body"}, {PublicName: "web_search_options", WireName: "web_search_options", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("chat_delete-completion",
			mcplib.WithDescription("Delete a stored chat completion. Only Chat Completions that have been created with the `store` parameter set to `true` can be deleted. Required: completion_id. Returns the ChatCompletionDeleted. Destructive."),
			mcplib.WithString("completion_id", mcplib.Required(), mcplib.Description("The ID of the chat completion to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/chat/completions/{completion_id}", []mcpParamBinding{{PublicName: "completion_id", WireName: "completion_id", Location: "path"}}, []string{"completion_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chat_get-completion",
			mcplib.WithDescription("Get a stored chat completion. Only Chat Completions that have been created with the `store` parameter set to `true` will be returned. Required: completion_id. Returns the CreateChatCompletionResponse."),
			mcplib.WithString("completion_id", mcplib.Required(), mcplib.Description("The ID of the chat completion to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/chat/completions/{completion_id}", []mcpParamBinding{{PublicName: "completion_id", WireName: "completion_id", Location: "path"}}, []string{"completion_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chat_get-completion-messages",
			mcplib.WithDescription("Get the messages in a stored chat completion. Only Chat Completions that have been created with the `store` parameter set to `true` will be returned. Required: completion_id. Optional: after, limit (default: 20), order (default: asc). Returns array of ChatGetCompletionMessagesItem."),
			mcplib.WithString("completion_id", mcplib.Required(), mcplib.Description("The ID of the chat completion to retrieve messages from.")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last message from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of messages to retrieve.")),
			mcplib.WithString("order", mcplib.Description("Sort order for messages by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/chat/completions/{completion_id}/messages", []mcpParamBinding{{PublicName: "completion_id", WireName: "completion_id", Location: "path"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"completion_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chat_list-completions",
			mcplib.WithDescription("List stored Chat Completions. Only Chat Completions that have been stored with the `store` parameter set to `true` will be returned. Optional: model, metadata, after (plus 2 more). Returns array of CreateChatCompletionResponse."),
			mcplib.WithString("model", mcplib.Description("The model used to generate the Chat Completions.")),
			mcplib.WithString("metadata", mcplib.Description("A list of metadata keys to filter the Chat Completions by. Example: `metadata[key1]=value1&metadata[key2]=value2`")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last chat completion from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of Chat Completions to retrieve.")),
			mcplib.WithString("order", mcplib.Description("Sort order for Chat Completions by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/chat/completions", []mcpParamBinding{{PublicName: "model", WireName: "model", Location: "query"}, {PublicName: "metadata", WireName: "metadata", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("chat_update-completion",
			mcplib.WithDescription("Modify a stored chat completion. Only Chat Completions that have been created with the `store` parameter set to `true` can be modified. Currently, the only supported modification is to update the `metadata` field. Required: completion_id, metadata. Returns the new CreateChatCompletionResponse."),
			mcplib.WithString("completion_id", mcplib.Required(), mcplib.Description("The ID of the chat completion to update.")),
			mcplib.WithString("metadata", mcplib.Required(), mcplib.Description("Metadata")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/chat/completions/{completion_id}", []mcpParamBinding{{PublicName: "completion_id", WireName: "completion_id", Location: "path"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}}, []string{"completion_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chatkit_cancel-chat-session-method",
			mcplib.WithDescription("Cancel an active ChatKit session and return its most recent metadata. Cancelling prevents new requests from using the issued client secret. Required: session_id. Returns the new ChatSessionResource."),
			mcplib.WithString("session_id", mcplib.Required(), mcplib.Description("Unique identifier for the ChatKit session to cancel.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/chatkit/sessions/{session_id}/cancel", []mcpParamBinding{{PublicName: "session_id", WireName: "session_id", Location: "path"}}, []string{"session_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chatkit_create-chat-session-method",
			mcplib.WithDescription("Create a ChatKit session. Required: user, workflow. Optional: chatkit_configuration, expires_after, rate_limits. Returns the new ChatSessionResource."),
			mcplib.WithString("chatkit_configuration", mcplib.Description("Optional overrides for ChatKit runtime configuration features")),
			mcplib.WithString("expires_after", mcplib.Description("Optional override for session expiration timing in seconds from creation. Defaults to 10 minutes.")),
			mcplib.WithString("rate_limits", mcplib.Description("Optional override for per-minute request limits. When omitted, defaults to 10.")),
			mcplib.WithString("user", mcplib.Required(), mcplib.Description("A free-form string that identifies your end user; ensures this Session can access other objects that have the same...")),
			mcplib.WithString("workflow", mcplib.Required(), mcplib.Description("Workflow that powers the session.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/chatkit/sessions", []mcpParamBinding{{PublicName: "chatkit_configuration", WireName: "chatkit_configuration", Location: "body"}, {PublicName: "expires_after", WireName: "expires_after", Location: "body"}, {PublicName: "rate_limits", WireName: "rate_limits", Location: "body"}, {PublicName: "user", WireName: "user", Location: "body"}, {PublicName: "workflow", WireName: "workflow", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("chatkit_delete-thread-method",
			mcplib.WithDescription("Delete a ChatKit thread along with its items and stored attachments. Required: thread_id. Returns the DeletedThreadResource. Destructive."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Identifier of the ChatKit thread to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/chatkit/threads/{thread_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chatkit_get-thread-method",
			mcplib.WithDescription("Retrieve a ChatKit thread by its identifier. Required: thread_id. Returns the ThreadResource."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Identifier of the ChatKit thread to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/chatkit/threads/{thread_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chatkit_list-thread-items-method",
			mcplib.WithDescription("List items that belong to a ChatKit thread. Required: thread_id. Optional: limit, order, after (plus 1 more). Returns array of ThreadItem."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("Identifier of the ChatKit thread whose items are requested.")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of thread items to return. Defaults to 20.")),
			mcplib.WithString("order", mcplib.Description("Sort order for results by creation time. Defaults to `desc`.")),
			mcplib.WithString("after", mcplib.Description("List items created after this thread item ID. Defaults to null for the first page.")),
			mcplib.WithString("before", mcplib.Description("List items created before this thread item ID. Defaults to null for the newest results.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/chatkit/threads/{thread_id}/items", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("chatkit_list-threads-method",
			mcplib.WithDescription("List ChatKit threads with optional pagination and user filters. Optional: limit, order, after (plus 2 more). Returns array of ThreadResource."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of thread items to return. Defaults to 20.")),
			mcplib.WithString("order", mcplib.Description("Sort order for results by creation time. Defaults to `desc`.")),
			mcplib.WithString("after", mcplib.Description("List items created after this thread item ID. Defaults to null for the first page.")),
			mcplib.WithString("before", mcplib.Description("List items created before this thread item ID. Defaults to null for the newest results.")),
			mcplib.WithString("user", mcplib.Description("Filter threads that belong to this user identifier. Defaults to null to return all users.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/chatkit/threads", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "user", WireName: "user", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("completions_create",
			mcplib.WithDescription("Creates a completion for the provided prompt and parameters. Returns a completion object, or a sequence of completion objects if the request is streamed. Required: model, prompt (default: <|endoftext|>). Optional: best_of (default: 1), echo (default: false), frequency_penalty (default: 0) (plus 13 more)."),
			mcplib.WithNumber("best_of", mcplib.Description("Generates `best_of` completions server-side and returns the 'best' (the one with the highest log probability per...")),
			mcplib.WithBoolean("echo", mcplib.Description("Echo back the prompt in addition to the completion")),
			mcplib.WithNumber("frequency_penalty", mcplib.Description("Number between -2.0 and 2.0. Positive values penalize new tokens based on their existing frequency in the text so...")),
			mcplib.WithString("logit_bias", mcplib.Description("Modify the likelihood of specified tokens appearing in the completion. Accepts a JSON object that maps tokens...")),
			mcplib.WithNumber("logprobs", mcplib.Description("Include the log probabilities on the `logprobs` most likely output tokens, as well the chosen tokens. For example,...")),
			mcplib.WithNumber("max_tokens", mcplib.Description("The maximum number of [tokens](/tokenizer) that can be generated in the completion. The token count of your prompt...")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API...")),
			mcplib.WithNumber("n", mcplib.Description("How many completions to generate for each prompt. **Note:** Because this parameter generates many completions, it...")),
			mcplib.WithNumber("presence_penalty", mcplib.Description("Number between -2.0 and 2.0. Positive values penalize new tokens based on whether they appear in the text so far,...")),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("The prompt(s) to generate completions for, encoded as a string, array of strings, array of tokens, or array of token...")),
			mcplib.WithNumber("seed", mcplib.Description("If specified, our system will make a best effort to sample deterministically, such that repeated requests with the...")),
			mcplib.WithString("stop", mcplib.Description("Not supported with latest reasoning models `o3` and `o4-mini`. Up to 4 sequences where the API will stop generating...")),
			mcplib.WithBoolean("stream", mcplib.Description("Whether to stream back partial progress. If set, tokens will be sent as data-only [server-sent...")),
			mcplib.WithString("stream_options", mcplib.Description("Stream options")),
			mcplib.WithString("suffix", mcplib.Description("The suffix that comes after a completion of inserted text. This parameter is only supported for...")),
			mcplib.WithNumber("temperature", mcplib.Description("What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while...")),
			mcplib.WithNumber("top_p", mcplib.Description("An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the...")),
			mcplib.WithString("user", mcplib.Description("A unique identifier representing your end-user, which can help OpenAI to monitor and detect...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/completions", []mcpParamBinding{{PublicName: "best_of", WireName: "best_of", Location: "body"}, {PublicName: "echo", WireName: "echo", Location: "body"}, {PublicName: "frequency_penalty", WireName: "frequency_penalty", Location: "body"}, {PublicName: "logit_bias", WireName: "logit_bias", Location: "body"}, {PublicName: "logprobs", WireName: "logprobs", Location: "body"}, {PublicName: "max_tokens", WireName: "max_tokens", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "n", WireName: "n", Location: "body"}, {PublicName: "presence_penalty", WireName: "presence_penalty", Location: "body"}, {PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "seed", WireName: "seed", Location: "body"}, {PublicName: "stop", WireName: "stop", Location: "body"}, {PublicName: "stream", WireName: "stream", Location: "body"}, {PublicName: "stream_options", WireName: "stream_options", Location: "body"}, {PublicName: "suffix", WireName: "suffix", Location: "body"}, {PublicName: "temperature", WireName: "temperature", Location: "body"}, {PublicName: "top_p", WireName: "top_p", Location: "body"}, {PublicName: "user", WireName: "user", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("containers_create",
			mcplib.WithDescription("Creates a container. Required: name. Optional: expires_after, file_ids, memory_limit (plus 2 more). Returns the new ContainerResource."),
			mcplib.WithString("expires_after", mcplib.Description("Container expiration time in seconds relative to the 'anchor' time.")),
			mcplib.WithString("file_ids", mcplib.Description("IDs of files to copy to the container.")),
			mcplib.WithString("memory_limit", mcplib.Description("Optional memory limit for the container. Defaults to '1g'.")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name of the container to create.")),
			mcplib.WithString("network_policy", mcplib.Description("Network access policy for the container.")),
			mcplib.WithString("skills", mcplib.Description("An optional list of skills referenced by id or inline data.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/containers", []mcpParamBinding{{PublicName: "expires_after", WireName: "expires_after", Location: "body"}, {PublicName: "file_ids", WireName: "file_ids", Location: "body"}, {PublicName: "memory_limit", WireName: "memory_limit", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "network_policy", WireName: "network_policy", Location: "body"}, {PublicName: "skills", WireName: "skills", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("containers_delete",
			mcplib.WithDescription("Delete a container. Required: container_id. Destructive."),
			mcplib.WithString("container_id", mcplib.Required(), mcplib.Description("The ID of the container to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/containers/{container_id}", []mcpParamBinding{{PublicName: "container_id", WireName: "container_id", Location: "path"}}, []string{"container_id"}),
	)
	s.AddTool(
		mcplib.NewTool("containers_list",
			mcplib.WithDescription("Lists containers. Optional: limit (default: 20), order (default: desc), after (plus 1 more). Returns array of ContainerResource."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("name", mcplib.Description("Filter results by container name.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/containers", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "name", WireName: "name", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("containers_retrieve",
			mcplib.WithDescription("Retrieves a container. Required: container_id. Returns the ContainerResource."),
			mcplib.WithString("container_id", mcplib.Required(), mcplib.Description("Container id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/containers/{container_id}", []mcpParamBinding{{PublicName: "container_id", WireName: "container_id", Location: "path"}}, []string{"container_id"}),
	)
	s.AddTool(
		mcplib.NewTool("containers_files_create-container",
			mcplib.WithDescription("Create a Container File You can send either a multipart/form-data request with the raw file content, or a JSON request with a file ID. Required: container_id. Optional: file, file_id. Returns the new ContainerFileResource."),
			mcplib.WithString("container_id", mcplib.Required(), mcplib.Description("Container id")),
			mcplib.WithString("file", mcplib.Description("The File object (not file name) to be uploaded.")),
			mcplib.WithString("file_id", mcplib.Description("Name of the file to create.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/containers/{container_id}/files", []mcpParamBinding{{PublicName: "container_id", WireName: "container_id", Location: "path"}, {PublicName: "file", WireName: "file", Location: "body"}, {PublicName: "file_id", WireName: "file_id", Location: "body"}}, []string{"container_id"}),
	)
	s.AddTool(
		mcplib.NewTool("containers_files_delete-container",
			mcplib.WithDescription("Delete a container file. Required: container_id, file_id. Destructive."),
			mcplib.WithString("container_id", mcplib.Required(), mcplib.Description("Container id")),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("File id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/containers/{container_id}/files/{file_id}", []mcpParamBinding{{PublicName: "container_id", WireName: "container_id", Location: "path"}, {PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"container_id", "file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("containers_files_list-container",
			mcplib.WithDescription("Lists container files. Required: container_id. Optional: limit (default: 20), order (default: desc), after. Returns array of ContainerFileResource."),
			mcplib.WithString("container_id", mcplib.Required(), mcplib.Description("Container id")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/containers/{container_id}/files", []mcpParamBinding{{PublicName: "container_id", WireName: "container_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{"container_id"}),
	)
	s.AddTool(
		mcplib.NewTool("containers_files_retrieve-container",
			mcplib.WithDescription("Retrieves a container file. Required: container_id, file_id. Returns the ContainerFileResource."),
			mcplib.WithString("container_id", mcplib.Required(), mcplib.Description("Container id")),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("File id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/containers/{container_id}/files/{file_id}", []mcpParamBinding{{PublicName: "container_id", WireName: "container_id", Location: "path"}, {PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"container_id", "file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("containers_files_retrieve-container-content",
			mcplib.WithDescription("Retrieves a container file content. Required: container_id, file_id."),
			mcplib.WithString("container_id", mcplib.Required(), mcplib.Description("Container id")),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("File id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/containers/{container_id}/files/{file_id}/content", []mcpParamBinding{{PublicName: "container_id", WireName: "container_id", Location: "path"}, {PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"container_id", "file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_create",
			mcplib.WithDescription("Create a conversation. Optional: items, metadata. Returns the new ConversationResource."),
			mcplib.WithString("items", mcplib.Description("Items")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/conversations", []mcpParamBinding{{PublicName: "items", WireName: "items", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_delete",
			mcplib.WithDescription("Delete a conversation. Items in the conversation will not be deleted. Required: conversation_id. Returns the DeletedConversationResource. Destructive."),
			mcplib.WithString("conversation_id", mcplib.Required(), mcplib.Description("The ID of the conversation to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/conversations/{conversation_id}", []mcpParamBinding{{PublicName: "conversation_id", WireName: "conversation_id", Location: "path"}}, []string{"conversation_id"}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_get",
			mcplib.WithDescription("Get a conversation. Required: conversation_id. Returns the ConversationResource."),
			mcplib.WithString("conversation_id", mcplib.Required(), mcplib.Description("The ID of the conversation to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/conversations/{conversation_id}", []mcpParamBinding{{PublicName: "conversation_id", WireName: "conversation_id", Location: "path"}}, []string{"conversation_id"}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_update",
			mcplib.WithDescription("Update a conversation. Required: conversation_id, metadata. Returns the new ConversationResource."),
			mcplib.WithString("conversation_id", mcplib.Required(), mcplib.Description("The ID of the conversation to update.")),
			mcplib.WithString("metadata", mcplib.Required(), mcplib.Description("Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/conversations/{conversation_id}", []mcpParamBinding{{PublicName: "conversation_id", WireName: "conversation_id", Location: "path"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}}, []string{"conversation_id"}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_items_create-conversation",
			mcplib.WithDescription("Create items in a conversation with the given ID. Required: conversation_id, items. Optional: include. Returns array of ConversationItem."),
			mcplib.WithString("conversation_id", mcplib.Required(), mcplib.Description("The ID of the conversation to add the item to.")),
			mcplib.WithString("include", mcplib.Description("Additional fields to include in the response. See the `include` parameter for [listing Conversation items...")),
			mcplib.WithString("items", mcplib.Required(), mcplib.Description("The items to add to the conversation. You may add up to 20 items at a time.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/conversations/{conversation_id}/items", []mcpParamBinding{{PublicName: "conversation_id", WireName: "conversation_id", Location: "path"}, {PublicName: "include", WireName: "include", Location: "query"}, {PublicName: "items", WireName: "items", Location: "body"}}, []string{"conversation_id"}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_items_delete-conversation",
			mcplib.WithDescription("Delete an item from a conversation with the given IDs. Required: conversation_id, item_id. Returns the ConversationResource. Destructive."),
			mcplib.WithString("conversation_id", mcplib.Required(), mcplib.Description("The ID of the conversation that contains the item.")),
			mcplib.WithString("item_id", mcplib.Required(), mcplib.Description("The ID of the item to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/conversations/{conversation_id}/items/{item_id}", []mcpParamBinding{{PublicName: "conversation_id", WireName: "conversation_id", Location: "path"}, {PublicName: "item_id", WireName: "item_id", Location: "path"}}, []string{"conversation_id", "item_id"}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_items_get-conversation",
			mcplib.WithDescription("Get a single item from a conversation with the given IDs. Required: conversation_id, item_id. Optional: include."),
			mcplib.WithString("conversation_id", mcplib.Required(), mcplib.Description("The ID of the conversation that contains the item.")),
			mcplib.WithString("item_id", mcplib.Required(), mcplib.Description("The ID of the item to retrieve.")),
			mcplib.WithString("include", mcplib.Description("Additional fields to include in the response. See the `include` parameter for [listing Conversation items...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/conversations/{conversation_id}/items/{item_id}", []mcpParamBinding{{PublicName: "conversation_id", WireName: "conversation_id", Location: "path"}, {PublicName: "item_id", WireName: "item_id", Location: "path"}, {PublicName: "include", WireName: "include", Location: "query"}}, []string{"conversation_id", "item_id"}),
	)
	s.AddTool(
		mcplib.NewTool("conversations_items_list-conversation",
			mcplib.WithDescription("List all items for a conversation with the given ID. Required: conversation_id. Optional: limit (default: 20), order, after (plus 1 more). Returns array of ConversationItem."),
			mcplib.WithString("conversation_id", mcplib.Required(), mcplib.Description("The ID of the conversation to list items for.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("The order to return the input items in. Default is `desc`. - `asc`: Return the input items in ascending order. -...")),
			mcplib.WithString("after", mcplib.Description("An item ID to list items after, used in pagination.")),
			mcplib.WithString("include", mcplib.Description("Specify additional output data to include in the model response. Currently supported values are: -...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/conversations/{conversation_id}/items", []mcpParamBinding{{PublicName: "conversation_id", WireName: "conversation_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "include", WireName: "include", Location: "query"}}, []string{"conversation_id"}),
	)
	s.AddTool(
		mcplib.NewTool("embeddings_create",
			mcplib.WithDescription("Creates an embedding vector representing the input text. Required: input, model. Optional: dimensions, encoding_format (default: float), user. Returns array of Embedding."),
			mcplib.WithNumber("dimensions", mcplib.Description("The number of dimensions the resulting output embeddings should have. Only supported in `text-embedding-3` and later...")),
			mcplib.WithString("encoding_format", mcplib.Description("The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/).")),
			mcplib.WithString("input", mcplib.Required(), mcplib.Description("Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an...")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API...")),
			mcplib.WithString("user", mcplib.Description("A unique identifier representing your end-user, which can help OpenAI to monitor and detect...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/embeddings", []mcpParamBinding{{PublicName: "dimensions", WireName: "dimensions", Location: "body"}, {PublicName: "encoding_format", WireName: "encoding_format", Location: "body"}, {PublicName: "input", WireName: "input", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "user", WireName: "user", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("evals_create",
			mcplib.WithDescription("Create the structure of an evaluation that can be used to test a model's performance. An evaluation is a set of testing criteria and the config for a data source, which dictates the schema of the data used in the evaluation. After creating an evaluation, you can run it on different models and model parameters. We support several types of graders and datasources. For more information, see the [Evals guide](/docs/guides/evals). Required: data_source_config, testing_criteria. Optional: metadata, name. Returns the new Eval."),
			mcplib.WithString("data_source_config", mcplib.Required(), mcplib.Description("The configuration for the data source used for the evaluation runs. Dictates the schema of the data used in the...")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("name", mcplib.Description("The name of the evaluation.")),
			mcplib.WithString("testing_criteria", mcplib.Required(), mcplib.Description("A list of graders for all eval runs in this group. Graders can reference variables in the data source using double...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/evals", []mcpParamBinding{{PublicName: "data_source_config", WireName: "data_source_config", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}, {PublicName: "testing_criteria", WireName: "testing_criteria", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("evals_delete",
			mcplib.WithDescription("Delete an evaluation. Required: eval_id. Returns the EvalsDeleteResponse. Destructive."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/evals/{eval_id}", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}}, []string{"eval_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_get",
			mcplib.WithDescription("Get an evaluation by ID. Required: eval_id. Returns the Eval."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/evals/{eval_id}", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}}, []string{"eval_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_list",
			mcplib.WithDescription("List evaluations for a project. Optional: after, limit (default: 20), order (default: asc) (plus 1 more). Returns array of Eval."),
			mcplib.WithString("after", mcplib.Description("Identifier for the last eval from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of evals to retrieve.")),
			mcplib.WithString("order", mcplib.Description("Sort order for evals by timestamp. Use `asc` for ascending order or `desc` for descending order.")),
			mcplib.WithString("order_by", mcplib.Description("Evals can be ordered by creation time or last updated time. Use `created_at` for creation time or `updated_at` for...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/evals", []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "order_by", WireName: "order_by", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("evals_update",
			mcplib.WithDescription("Update certain properties of an evaluation. Required: eval_id. Optional: metadata, name. Returns the new Eval."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to update.")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("name", mcplib.Description("Rename the evaluation.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/evals/{eval_id}", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"eval_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_runs_cancel-eval",
			mcplib.WithDescription("Cancel an ongoing evaluation run. Required: eval_id, run_id. Returns the new EvalRun."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation whose run you want to cancel.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to cancel.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/evals/{eval_id}/runs/{run_id}", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}}, []string{"eval_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_runs_create-eval",
			mcplib.WithDescription("Kicks off a new run for a given evaluation, specifying the data source, and what model configuration to use to test. The datasource will be validated against the schema specified in the config of the evaluation. Required: eval_id, data_source. Optional: metadata, name. Returns the new EvalRun."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to create a run for.")),
			mcplib.WithString("data_source", mcplib.Required(), mcplib.Description("Details about the run's data source.")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("name", mcplib.Description("The name of the run.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/evals/{eval_id}/runs", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}, {PublicName: "data_source", WireName: "data_source", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"eval_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_runs_delete-eval",
			mcplib.WithDescription("Delete an eval run. Required: eval_id, run_id. Returns the RunsDeleteEvalResponse. Destructive."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to delete the run from.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/evals/{eval_id}/runs/{run_id}", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}}, []string{"eval_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_runs_get-eval",
			mcplib.WithDescription("Get a list of runs for an evaluation. Required: eval_id. Optional: after, limit (default: 20), order (default: asc) (plus 1 more). Returns array of EvalRun."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to retrieve runs for.")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last run from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of runs to retrieve.")),
			mcplib.WithString("order", mcplib.Description("Sort order for runs by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.")),
			mcplib.WithString("status", mcplib.Description("Filter runs by status. One of `queued` | `in_progress` | `failed` | `completed` | `canceled`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/evals/{eval_id}/runs", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "status", WireName: "status", Location: "query"}}, []string{"eval_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_runs_get-eval-evals",
			mcplib.WithDescription("Get an evaluation run by ID. Required: eval_id, run_id. Returns the EvalRun."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to retrieve runs for.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/evals/{eval_id}/runs/{run_id}", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}}, []string{"eval_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_runs_get-eval-output-item",
			mcplib.WithDescription("Get an evaluation run output item by ID. Required: eval_id, run_id, output_item_id. Returns the EvalRunOutputItem."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to retrieve runs for.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to retrieve.")),
			mcplib.WithString("output_item_id", mcplib.Required(), mcplib.Description("The ID of the output item to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/evals/{eval_id}/runs/{run_id}/output_items/{output_item_id}", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}, {PublicName: "output_item_id", WireName: "output_item_id", Location: "path"}}, []string{"eval_id", "run_id", "output_item_id"}),
	)
	s.AddTool(
		mcplib.NewTool("evals_runs_get-eval-output-items",
			mcplib.WithDescription("Get a list of output items for an evaluation run. Required: eval_id, run_id. Optional: after, limit (default: 20), status (plus 1 more). Returns array of EvalRunOutputItem."),
			mcplib.WithString("eval_id", mcplib.Required(), mcplib.Description("The ID of the evaluation to retrieve runs for.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to retrieve output items for.")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last output item from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of output items to retrieve.")),
			mcplib.WithString("status", mcplib.Description("Filter output items by status. Use `failed` to filter by failed output items or `pass` to filter by passed output items.")),
			mcplib.WithString("order", mcplib.Description("Sort order for output items by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/evals/{eval_id}/runs/{run_id}/output_items", []mcpParamBinding{{PublicName: "eval_id", WireName: "eval_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "status", WireName: "status", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"eval_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("files_create",
			mcplib.WithDescription("Upload a file that can be used across various endpoints. Individual files can be up to 512 MB, and each project can store up to 2.5 TB of files in total. There is no organization-wide storage limit. Uploads to this endpoint are rate-limited to 1,000 requests per minute per authenticated user. - The Assistants API supports files up to 2 million tokens and of specific file types. See the [Assistants Tools guide](/docs/assistants/tools) for details. - The Fine-tuning API only supports `.jsonl` files. The input also has certain required formats for fine-tuning [chat](/docs/api-reference/fine-tuning/chat-input) or [completions](/docs/api-reference/fine-tuning/completions-input) models. - The Batch API only supports `.jsonl` files up to 200 MB in size. The input also has a specific required [format](/docs/api-reference/batch/request-input). - For Retrieval or `file_search` ingestion, upload files here first. If you need to attach multiple uploaded files to the same vector store, use [`/vector_stores/{vector_store_id}/file_batches`](/docs/api-reference/vector-stores-file-batches/createBatch) instead of attaching them one by one. Vector store attachment has separate limits from file upload, including 2,000 attached files per minute per organization. Please [contact us](https://help.openai.com/) if you need to increase these storage limits. Required: file, purpose. Optional: expires_after. Returns the new OpenAifile."),
			mcplib.WithString("expires_after", mcplib.Description("The expiration policy for a file. By default, files with `purpose=batch` expire after 30 days and all other files...")),
			mcplib.WithString("file", mcplib.Required(), mcplib.Description("The File object (not file name) to be uploaded.")),
			mcplib.WithString("purpose", mcplib.Required(), mcplib.Description("The intended purpose of the uploaded file. One of: - `assistants`: Used in the Assistants API - `batch`: Used in the...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/files", []mcpParamBinding{{PublicName: "expires_after", WireName: "expires_after", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "file", WireName: "file", Location: "body", Format: "binary", RequestContentType: "multipart/form-data"}, {PublicName: "purpose", WireName: "purpose", Location: "body", RequestContentType: "multipart/form-data"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_delete",
			mcplib.WithDescription("Delete a file and remove it from all vector stores. Required: file_id. Returns the DeleteFileResponse. Destructive."),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("The ID of the file to use for this request.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/files/{file_id}", []mcpParamBinding{{PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("files_list",
			mcplib.WithDescription("Returns a list of files. Optional: purpose, limit (default: 10000), order (default: desc) (plus 1 more)."),
			mcplib.WithString("purpose", mcplib.Description("Only return files with the given purpose.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned. Limit can range between 1 and 10,000, and the default is 10,000.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/files", []mcpParamBinding{{PublicName: "purpose", WireName: "purpose", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_retrieve",
			mcplib.WithDescription("Returns information about a specific file. Required: file_id."),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("The ID of the file to use for this request.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/files/{file_id}", []mcpParamBinding{{PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("files_content_download-file",
			mcplib.WithDescription("Returns the contents of the specified file. Required: file_id."),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("The ID of the file to use for this request.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/files/{file_id}/content", []mcpParamBinding{{PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_cancel-job",
			mcplib.WithDescription("Immediately cancel a fine-tune job. Required: fine_tuning_job_id. Returns the new FineTuningJob."),
			mcplib.WithString("fine_tuning_job_id", mcplib.Required(), mcplib.Description("The ID of the fine-tuning job to cancel.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/fine_tuning/jobs/{fine_tuning_job_id}/cancel", []mcpParamBinding{{PublicName: "fine_tuning_job_id", WireName: "fine_tuning_job_id", Location: "path"}}, []string{"fine_tuning_job_id"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_create-checkpoint-permission",
			mcplib.WithDescription("**NOTE:** Calling this endpoint requires an [admin API key](../admin-api-keys). This enables organization owners to share fine-tuned models with other projects in their organization. Required: fine_tuned_model_checkpoint, project_ids. Returns array of FineTuningCheckpointPermission."),
			mcplib.WithString("fine_tuned_model_checkpoint", mcplib.Required(), mcplib.Description("The ID of the fine-tuned model checkpoint to create a permission for.")),
			mcplib.WithString("project_ids", mcplib.Required(), mcplib.Description("The project identifiers to grant access to.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/fine_tuning/checkpoints/{fine_tuned_model_checkpoint}/permissions", []mcpParamBinding{{PublicName: "fine_tuned_model_checkpoint", WireName: "fine_tuned_model_checkpoint", Location: "path"}, {PublicName: "project_ids", WireName: "project_ids", Location: "body"}}, []string{"fine_tuned_model_checkpoint"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_create-job",
			mcplib.WithDescription("Creates a fine-tuning job which begins the process of creating a new model from a given dataset. Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete. [Learn more about fine-tuning](/docs/guides/model-optimization). Required: model, training_file. Optional: hyperparameters, integrations, metadata (plus 4 more). Returns the new FineTuningJob."),
			mcplib.WithString("hyperparameters", mcplib.Description("The hyperparameters used for the fine-tuning job. This value is now deprecated in favor of `method`, and should be...")),
			mcplib.WithString("integrations", mcplib.Description("A list of integrations to enable for your fine-tuning job.")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("method", mcplib.Description("The method used for fine-tuning.")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("The name of the model to fine-tune. You can select one of the [supported...")),
			mcplib.WithNumber("seed", mcplib.Description("The seed controls the reproducibility of the job. Passing in the same seed and job parameters should produce the...")),
			mcplib.WithString("suffix", mcplib.Description("A string of up to 64 characters that will be added to your fine-tuned model name. For example, a `suffix` of...")),
			mcplib.WithString("training_file", mcplib.Required(), mcplib.Description("The ID of an uploaded file that contains training data. See [upload file](/docs/api-reference/files/create) for how...")),
			mcplib.WithString("validation_file", mcplib.Description("The ID of an uploaded file that contains validation data. If you provide this file, the data is used to generate...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/fine_tuning/jobs", []mcpParamBinding{{PublicName: "hyperparameters", WireName: "hyperparameters", Location: "body"}, {PublicName: "integrations", WireName: "integrations", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "seed", WireName: "seed", Location: "body"}, {PublicName: "suffix", WireName: "suffix", Location: "body"}, {PublicName: "training_file", WireName: "training_file", Location: "body"}, {PublicName: "validation_file", WireName: "validation_file", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_delete-checkpoint-permission",
			mcplib.WithDescription("**NOTE:** This endpoint requires an [admin API key](../admin-api-keys). Organization owners can use this endpoint to delete a permission for a fine-tuned model checkpoint. Required: fine_tuned_model_checkpoint, permission_id. Returns the DeleteFineTuningCheckpointPermissionResponse. Destructive."),
			mcplib.WithString("fine_tuned_model_checkpoint", mcplib.Required(), mcplib.Description("The ID of the fine-tuned model checkpoint to delete a permission for.")),
			mcplib.WithString("permission_id", mcplib.Required(), mcplib.Description("The ID of the fine-tuned model checkpoint permission to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/fine_tuning/checkpoints/{fine_tuned_model_checkpoint}/permissions/{permission_id}", []mcpParamBinding{{PublicName: "fine_tuned_model_checkpoint", WireName: "fine_tuned_model_checkpoint", Location: "path"}, {PublicName: "permission_id", WireName: "permission_id", Location: "path"}}, []string{"fine_tuned_model_checkpoint", "permission_id"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_list-checkpoint-permissions",
			mcplib.WithDescription("**NOTE:** This endpoint requires an [admin API key](../admin-api-keys). Organization owners can use this endpoint to view all permissions for a fine-tuned model checkpoint. Required: fine_tuned_model_checkpoint. Optional: project_id, after, limit (default: 10) (plus 1 more). Returns array of FineTuningCheckpointPermission."),
			mcplib.WithString("fine_tuned_model_checkpoint", mcplib.Required(), mcplib.Description("The ID of the fine-tuned model checkpoint to get permissions for.")),
			mcplib.WithString("project_id", mcplib.Description("The ID of the project to get permissions for.")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last permission ID from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of permissions to retrieve.")),
			mcplib.WithString("order", mcplib.Description("The order in which to retrieve permissions.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/fine_tuning/checkpoints/{fine_tuned_model_checkpoint}/permissions", []mcpParamBinding{{PublicName: "fine_tuned_model_checkpoint", WireName: "fine_tuned_model_checkpoint", Location: "path"}, {PublicName: "project_id", WireName: "project_id", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"fine_tuned_model_checkpoint"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_list-events",
			mcplib.WithDescription("Get status updates for a fine-tuning job. Required: fine_tuning_job_id. Optional: after, limit (default: 20). Returns array of FineTuningJobEvent."),
			mcplib.WithString("fine_tuning_job_id", mcplib.Required(), mcplib.Description("The ID of the fine-tuning job to get events for.")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last event from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of events to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/fine_tuning/jobs/{fine_tuning_job_id}/events", []mcpParamBinding{{PublicName: "fine_tuning_job_id", WireName: "fine_tuning_job_id", Location: "path"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{"fine_tuning_job_id"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_list-job-checkpoints",
			mcplib.WithDescription("List checkpoints for a fine-tuning job. Required: fine_tuning_job_id. Optional: after, limit (default: 10). Returns array of FineTuningJobCheckpoint."),
			mcplib.WithString("fine_tuning_job_id", mcplib.Required(), mcplib.Description("The ID of the fine-tuning job to get checkpoints for.")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last checkpoint ID from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of checkpoints to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/fine_tuning/jobs/{fine_tuning_job_id}/checkpoints", []mcpParamBinding{{PublicName: "fine_tuning_job_id", WireName: "fine_tuning_job_id", Location: "path"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{"fine_tuning_job_id"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_list-paginated-jobs",
			mcplib.WithDescription("List your organization's fine-tuning jobs. Optional: after, limit (default: 20), metadata. Returns array of FineTuningJob."),
			mcplib.WithString("after", mcplib.Description("Identifier for the last job from the previous pagination request.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of fine-tuning jobs to retrieve.")),
			mcplib.WithString("metadata", mcplib.Description("Optional metadata filter. To filter, use the syntax `metadata[k]=v`. Alternatively, set `metadata=null` to indicate...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/fine_tuning/jobs", []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "metadata", WireName: "metadata", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_pause-job",
			mcplib.WithDescription("Pause a fine-tune job. Required: fine_tuning_job_id. Returns the new FineTuningJob."),
			mcplib.WithString("fine_tuning_job_id", mcplib.Required(), mcplib.Description("The ID of the fine-tuning job to pause.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/fine_tuning/jobs/{fine_tuning_job_id}/pause", []mcpParamBinding{{PublicName: "fine_tuning_job_id", WireName: "fine_tuning_job_id", Location: "path"}}, []string{"fine_tuning_job_id"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_resume-job",
			mcplib.WithDescription("Resume a fine-tune job. Required: fine_tuning_job_id. Returns the new FineTuningJob."),
			mcplib.WithString("fine_tuning_job_id", mcplib.Required(), mcplib.Description("The ID of the fine-tuning job to resume.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/fine_tuning/jobs/{fine_tuning_job_id}/resume", []mcpParamBinding{{PublicName: "fine_tuning_job_id", WireName: "fine_tuning_job_id", Location: "path"}}, []string{"fine_tuning_job_id"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_retrieve-job",
			mcplib.WithDescription("Get info about a fine-tuning job. [Learn more about fine-tuning](/docs/guides/model-optimization). Required: fine_tuning_job_id. Returns the FineTuningJob."),
			mcplib.WithString("fine_tuning_job_id", mcplib.Required(), mcplib.Description("The ID of the fine-tuning job.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/fine_tuning/jobs/{fine_tuning_job_id}", []mcpParamBinding{{PublicName: "fine_tuning_job_id", WireName: "fine_tuning_job_id", Location: "path"}}, []string{"fine_tuning_job_id"}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_run-grader",
			mcplib.WithDescription("Run a grader. Required: grader, model_sample. Optional: item. Returns the new RunGraderResponse."),
			mcplib.WithString("grader", mcplib.Required(), mcplib.Description("The grader used for the fine-tuning job.")),
			mcplib.WithString("item", mcplib.Description("The dataset item provided to the grader. This will be used to populate the `item` namespace. See [the...")),
			mcplib.WithString("model_sample", mcplib.Required(), mcplib.Description("The model sample to be evaluated. This value will be used to populate the `sample` namespace. See [the...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/fine_tuning/alpha/graders/run", []mcpParamBinding{{PublicName: "grader", WireName: "grader", Location: "body"}, {PublicName: "item", WireName: "item", Location: "body"}, {PublicName: "model_sample", WireName: "model_sample", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("fine-tuning_validate-grader",
			mcplib.WithDescription("Validate a grader. Required: grader. Returns the new ValidateGraderResponse."),
			mcplib.WithString("grader", mcplib.Required(), mcplib.Description("The grader used for the fine-tuning job.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/fine_tuning/alpha/graders/validate", []mcpParamBinding{{PublicName: "grader", WireName: "grader", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("images_create",
			mcplib.WithDescription("Creates an image given a prompt. [Learn more](/docs/guides/images). Required: prompt. Optional: background (default: auto), model (default: dall-e-2), moderation (default: auto) (plus 10 more). Returns array of Image."),
			mcplib.WithString("background", mcplib.Description("Allows to set transparency for the background of the generated image(s). This parameter is only supported for the...")),
			mcplib.WithString("model", mcplib.Description("The model to use for image generation. One of `dall-e-2`, `dall-e-3`, or a GPT image model (`gpt-image-1`,...")),
			mcplib.WithString("moderation", mcplib.Description("Control the content-moderation level for images generated by the GPT image models. Must be either `low` for less...")),
			mcplib.WithNumber("n", mcplib.Description("The number of images to generate. Must be between 1 and 10. For `dall-e-3`, only `n=1` is supported.")),
			mcplib.WithNumber("output_compression", mcplib.Description("The compression level (0-100%) for the generated images. This parameter is only supported for the GPT image models...")),
			mcplib.WithString("output_format", mcplib.Description("The format in which the generated images are returned. This parameter is only supported for the GPT image models....")),
			mcplib.WithString("partial_images", mcplib.Description("Partial images")),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("A text description of the desired image(s). The maximum length is 32000 characters for the GPT image models, 1000...")),
			mcplib.WithString("quality", mcplib.Description("The quality of the image that will be generated. - `auto` (default value) will automatically select the best quality...")),
			mcplib.WithString("response_format", mcplib.Description("The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or...")),
			mcplib.WithString("size", mcplib.Description("The size of the generated images. For `gpt-image-2` and `gpt-image-2-2026-04-21`, arbitrary resolutions are...")),
			mcplib.WithBoolean("stream", mcplib.Description("Generate the image in streaming mode. Defaults to `false`. See the [Image generation...")),
			mcplib.WithString("style", mcplib.Description("The style of the generated images. This parameter is only supported for `dall-e-3`. Must be one of `vivid` or...")),
			mcplib.WithString("user", mcplib.Description("A unique identifier representing your end-user, which can help OpenAI to monitor and detect...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/images/generations", []mcpParamBinding{{PublicName: "background", WireName: "background", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "moderation", WireName: "moderation", Location: "body"}, {PublicName: "n", WireName: "n", Location: "body"}, {PublicName: "output_compression", WireName: "output_compression", Location: "body"}, {PublicName: "output_format", WireName: "output_format", Location: "body"}, {PublicName: "partial_images", WireName: "partial_images", Location: "body"}, {PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "quality", WireName: "quality", Location: "body"}, {PublicName: "response_format", WireName: "response_format", Location: "body"}, {PublicName: "size", WireName: "size", Location: "body"}, {PublicName: "stream", WireName: "stream", Location: "body"}, {PublicName: "style", WireName: "style", Location: "body"}, {PublicName: "user", WireName: "user", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("images_create-edit",
			mcplib.WithDescription("You can call this endpoint with either: - `multipart/form-data`: use binary uploads via `image` (and optional `mask`). - `application/json`: use `images` (and optional `mask`) as references with either `image_url` or `file_id`. Note that JSON requests use `images` (array) instead of the multipart `image` field. Required: images, prompt. Optional: background (default: auto), input_fidelity, mask (plus 10 more). Returns array of Image."),
			mcplib.WithString("background", mcplib.Description("Background behavior for generated image output.")),
			mcplib.WithString("images", mcplib.Required(), mcplib.Description("Input image references to edit. For GPT image models, you can provide up to 16 images.")),
			mcplib.WithString("input_fidelity", mcplib.Description("Controls fidelity to the original input image(s).")),
			mcplib.WithString("mask", mcplib.Description("Reference an input image by either URL or uploaded file ID. Provide exactly one of `image_url` or `file_id`.")),
			mcplib.WithString("model", mcplib.Description("The model to use for image editing.")),
			mcplib.WithString("moderation", mcplib.Description("Moderation level for GPT image models.")),
			mcplib.WithString("n", mcplib.Description("The number of edited images to generate.")),
			mcplib.WithString("output_compression", mcplib.Description("Compression level for `jpeg` or `webp` output.")),
			mcplib.WithString("output_format", mcplib.Description("Output image format. Supported for GPT image models.")),
			mcplib.WithString("partial_images", mcplib.Description("Partial images")),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("A text description of the desired image edit.")),
			mcplib.WithString("quality", mcplib.Description("Output quality for GPT image models.")),
			mcplib.WithString("size", mcplib.Description("Requested output image size.")),
			mcplib.WithString("stream", mcplib.Description("Stream partial image results as events.")),
			mcplib.WithString("user", mcplib.Description("A unique identifier representing your end-user, which can help OpenAI monitor and detect abuse.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/images/edits", []mcpParamBinding{{PublicName: "background", WireName: "background", Location: "body"}, {PublicName: "images", WireName: "images", Location: "body"}, {PublicName: "input_fidelity", WireName: "input_fidelity", Location: "body"}, {PublicName: "mask", WireName: "mask", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "moderation", WireName: "moderation", Location: "body"}, {PublicName: "n", WireName: "n", Location: "body"}, {PublicName: "output_compression", WireName: "output_compression", Location: "body"}, {PublicName: "output_format", WireName: "output_format", Location: "body"}, {PublicName: "partial_images", WireName: "partial_images", Location: "body"}, {PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "quality", WireName: "quality", Location: "body"}, {PublicName: "size", WireName: "size", Location: "body"}, {PublicName: "stream", WireName: "stream", Location: "body"}, {PublicName: "user", WireName: "user", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("images_create-variation",
			mcplib.WithDescription("Creates a variation of a given image. This endpoint only supports `dall-e-2`. Required: image. Optional: model (default: dall-e-2), n (default: 1), response_format (default: url) (plus 2 more). Returns array of Image."),
			mcplib.WithString("image", mcplib.Required(), mcplib.Description("The image to use as the basis for the variation(s). Must be a valid PNG file, less than 4MB, and square.")),
			mcplib.WithString("model", mcplib.Description("The model to use for image generation. Only `dall-e-2` is supported at this time.")),
			mcplib.WithNumber("n", mcplib.Description("The number of images to generate. Must be between 1 and 10.")),
			mcplib.WithString("response_format", mcplib.Description("The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for...")),
			mcplib.WithString("size", mcplib.Description("The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`.")),
			mcplib.WithString("user", mcplib.Description("A unique identifier representing your end-user, which can help OpenAI to monitor and detect...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/images/variations", []mcpParamBinding{{PublicName: "image", WireName: "image", Location: "body", Format: "binary", RequestContentType: "multipart/form-data"}, {PublicName: "model", WireName: "model", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "n", WireName: "n", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "response_format", WireName: "response_format", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "size", WireName: "size", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "user", WireName: "user", Location: "body", RequestContentType: "multipart/form-data"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("models_delete",
			mcplib.WithDescription("Delete a fine-tuned model. You must have the Owner role in your organization to delete a model. Required: model. Returns the DeleteModelResponse. Destructive."),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("The model to delete")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/models/{model}", []mcpParamBinding{{PublicName: "model", WireName: "model", Location: "path"}}, []string{"model"}),
	)
	s.AddTool(
		mcplib.NewTool("models_list",
			mcplib.WithDescription("Lists the currently available models, and provides basic information about each one such as the owner and availability. Returns array of Model."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/models", []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("models_retrieve",
			mcplib.WithDescription("Retrieves a model instance, providing basic information about the model such as the owner and permissioning. Required: model. Returns the Model."),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("The ID of the model to use for this request")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/models/{model}", []mcpParamBinding{{PublicName: "model", WireName: "model", Location: "path"}}, []string{"model"}),
	)
	s.AddTool(
		mcplib.NewTool("moderations_create",
			mcplib.WithDescription("Classifies if text and/or image inputs are potentially harmful. Learn more in the [moderation guide](/docs/guides/moderation). Required: input. Optional: model (default: omni-moderation-latest). Returns the new CreateModerationResponse."),
			mcplib.WithString("input", mcplib.Required(), mcplib.Description("Input (or inputs) to classify. Can be a single string, an array of strings, or an array of multi-modal input objects...")),
			mcplib.WithString("model", mcplib.Description("The content moderation model you would like to use. Learn more in [the moderation guide](/docs/guides/moderation),...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/moderations", []mcpParamBinding{{PublicName: "input", WireName: "input", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_activate-certificates",
			mcplib.WithDescription("Activate certificates at the organization level. You can atomically and idempotently activate up to 10 certificates at a time. Required: certificate_ids. Returns array of OrganizationCertificate."),
			mcplib.WithString("certificate_ids", mcplib.Required(), mcplib.Description("Certificate ids")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/certificates/activate", []mcpParamBinding{{PublicName: "certificate_ids", WireName: "certificate_ids", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_activate-project-certificates",
			mcplib.WithDescription("Activate certificates at the project level. You can atomically and idempotently activate up to 10 certificates at a time. Required: project_id, certificate_ids. Returns array of OrganizationProjectCertificate."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("certificate_ids", mcplib.Required(), mcplib.Description("Certificate ids")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}/certificates/activate", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "certificate_ids", WireName: "certificate_ids", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_add-group-user",
			mcplib.WithDescription("Adds a user to a group. Required: group_id, user_id. Returns the new GroupUserAssignment."),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group to update.")),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("Identifier of the user to add to the group.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/groups/{group_id}/users", []mcpParamBinding{{PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "user_id", WireName: "user_id", Location: "body"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_add-project-group",
			mcplib.WithDescription("Grants a group access to a project. Required: project_id, group_id, role. Returns the new ProjectGroup."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to update.")),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("Identifier of the group to add to the project.")),
			mcplib.WithString("role", mcplib.Required(), mcplib.Description("Identifier of the project role to grant to the group.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}/groups", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "group_id", WireName: "group_id", Location: "body"}, {PublicName: "role", WireName: "role", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_admin-api-keys-create",
			mcplib.WithDescription("Create a new admin-level API key for the organization. Required: name. Returns the new AdminApiKeyCreateResponse."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Name")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/admin_api_keys", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_admin-api-keys-delete",
			mcplib.WithDescription("Delete an organization admin API key. Required: key_id. Returns the OrganizationAdminApiKeysDeleteResponse. Destructive."),
			mcplib.WithString("key_id", mcplib.Required(), mcplib.Description("Key id")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/admin_api_keys/{key_id}", []mcpParamBinding{{PublicName: "key_id", WireName: "key_id", Location: "path"}}, []string{"key_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_admin-api-keys-get",
			mcplib.WithDescription("Get details for a specific organization API key by its ID. Required: key_id. Returns the AdminApiKey."),
			mcplib.WithString("key_id", mcplib.Required(), mcplib.Description("Key id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/admin_api_keys/{key_id}", []mcpParamBinding{{PublicName: "key_id", WireName: "key_id", Location: "path"}}, []string{"key_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_admin-api-keys-list",
			mcplib.WithDescription("Retrieve a paginated list of organization admin API keys. Optional: after, order (default: asc), limit (default: 20). Returns array of AdminApiKey."),
			mcplib.WithString("after", mcplib.Description("After")),
			mcplib.WithString("order", mcplib.Description("Order")),
			mcplib.WithNumber("limit", mcplib.Description("Limit")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/admin_api_keys", []mcpParamBinding{{PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_archive-project",
			mcplib.WithDescription("Archives a project in the organization. Archived projects cannot be used or updated. Required: project_id. Returns the new Project."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}/archive", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_assign-group-role",
			mcplib.WithDescription("Assigns an organization role to a group within the organization. Required: group_id, role_id. Returns the new GroupRoleAssignment."),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group that should receive the organization role.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("Identifier of the role to assign.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/groups/{group_id}/roles", []mcpParamBinding{{PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "body"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_assign-user-role",
			mcplib.WithDescription("Assigns an organization role to a user within the organization. Required: user_id, role_id. Returns the new UserRoleAssignment."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user that should receive the organization role.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("Identifier of the role to assign.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/users/{user_id}/roles", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "body"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_create-group",
			mcplib.WithDescription("Creates a new group in the organization. Required: name. Returns the new GroupResponse."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Human readable name for the group.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/groups", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_create-project",
			mcplib.WithDescription("Create a new project in the organization. Projects can be created and archived, but cannot be deleted. Required: name. Optional: external_key_id, geography. Returns the new Project."),
			mcplib.WithString("external_key_id", mcplib.Description("External key ID to associate with the project.")),
			mcplib.WithString("geography", mcplib.Description("Create the project with the specified data residency region. Your organization must have access to Data residency...")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The friendly name of the project, this name appears in reports.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects", []mcpParamBinding{{PublicName: "external_key_id", WireName: "external_key_id", Location: "body"}, {PublicName: "geography", WireName: "geography", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_create-project-service-account",
			mcplib.WithDescription("Creates a new service account in the project. This also returns an unredacted API key for the service account. Required: project_id, name."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("The name of the service account being created.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}/service_accounts", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_create-project-user",
			mcplib.WithDescription("Adds a user to the project. Users must already be members of the organization to be added to a project. Required: project_id, role. Optional: email, user_id. Returns the new ProjectUser."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("email", mcplib.Description("Email of the user to add.")),
			mcplib.WithString("role", mcplib.Required(), mcplib.Description("`owner` or `member`")),
			mcplib.WithString("user_id", mcplib.Description("The ID of the user.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}/users", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "email", WireName: "email", Location: "body"}, {PublicName: "role", WireName: "role", Location: "body"}, {PublicName: "user_id", WireName: "user_id", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_create-role",
			mcplib.WithDescription("Creates a custom role for the organization. Required: permissions, role_name. Optional: description. Returns the new Role."),
			mcplib.WithString("description", mcplib.Description("Optional description of the role.")),
			mcplib.WithString("permissions", mcplib.Required(), mcplib.Description("Permissions to grant to the role.")),
			mcplib.WithString("role_name", mcplib.Required(), mcplib.Description("Unique name for the role.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/roles", []mcpParamBinding{{PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "permissions", WireName: "permissions", Location: "body"}, {PublicName: "role_name", WireName: "role_name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_deactivate-certificates",
			mcplib.WithDescription("Deactivate certificates at the organization level. You can atomically and idempotently deactivate up to 10 certificates at a time. Required: certificate_ids. Returns array of OrganizationCertificate."),
			mcplib.WithString("certificate_ids", mcplib.Required(), mcplib.Description("Certificate ids")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/certificates/deactivate", []mcpParamBinding{{PublicName: "certificate_ids", WireName: "certificate_ids", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_deactivate-project-certificates",
			mcplib.WithDescription("Deactivate certificates at the project level. You can atomically and idempotently deactivate up to 10 certificates at a time. Required: project_id, certificate_ids. Returns array of OrganizationProjectCertificate."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("certificate_ids", mcplib.Required(), mcplib.Description("Certificate ids")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}/certificates/deactivate", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "certificate_ids", WireName: "certificate_ids", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_delete-certificate",
			mcplib.WithDescription("Delete a certificate from the organization. The certificate must be inactive for the organization and all projects. Required: certificate_id. Returns the DeleteCertificateResponse. Destructive."),
			mcplib.WithString("certificate_id", mcplib.Required(), mcplib.Description("Unique ID of the certificate to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/certificates/{certificate_id}", []mcpParamBinding{{PublicName: "certificate_id", WireName: "certificate_id", Location: "path"}}, []string{"certificate_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_delete-group",
			mcplib.WithDescription("Deletes a group from the organization. Required: group_id. Returns the GroupDeletedResource. Destructive."),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/groups/{group_id}", []mcpParamBinding{{PublicName: "group_id", WireName: "group_id", Location: "path"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_delete-invite",
			mcplib.WithDescription("Delete an invite. If the invite has already been accepted, it cannot be deleted. Required: invite_id. Returns the InviteDeleteResponse. Destructive."),
			mcplib.WithString("invite_id", mcplib.Required(), mcplib.Description("The ID of the invite to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/invites/{invite_id}", []mcpParamBinding{{PublicName: "invite_id", WireName: "invite_id", Location: "path"}}, []string{"invite_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_delete-project-api-key",
			mcplib.WithDescription("Deletes an API key from the project. Returns confirmation of the key deletion, or an error if the key belonged to a service account. Required: project_id, api_key_id. Destructive."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("api_key_id", mcplib.Required(), mcplib.Description("The ID of the API key.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/projects/{project_id}/api_keys/{api_key_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "api_key_id", WireName: "api_key_id", Location: "path"}}, []string{"project_id", "api_key_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_delete-project-service-account",
			mcplib.WithDescription("Deletes a service account from the project. Returns confirmation of service account deletion, or an error if the project is archived (archived projects have no service accounts). Required: project_id, service_account_id. Destructive."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("service_account_id", mcplib.Required(), mcplib.Description("The ID of the service account.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/projects/{project_id}/service_accounts/{service_account_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "service_account_id", WireName: "service_account_id", Location: "path"}}, []string{"project_id", "service_account_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_delete-project-user",
			mcplib.WithDescription("Deletes a user from the project. Returns confirmation of project user deletion, or an error if the project is archived (archived projects have no users). Required: project_id, user_id. Destructive."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/projects/{project_id}/users/{user_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"project_id", "user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_delete-role",
			mcplib.WithDescription("Deletes a custom role from the organization. Required: role_id. Returns the RoleDeletedResource. Destructive."),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("The ID of the role to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/roles/{role_id}", []mcpParamBinding{{PublicName: "role_id", WireName: "role_id", Location: "path"}}, []string{"role_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_delete-user",
			mcplib.WithDescription("Deletes a user from the organization. Required: user_id. Returns the UserDeleteResponse. Destructive."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/users/{user_id}", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_get-certificate",
			mcplib.WithDescription("Get a certificate that has been uploaded to the organization. You can get a certificate regardless of whether it is active or not. Required: certificate_id. Optional: include. Returns the Certificate."),
			mcplib.WithString("certificate_id", mcplib.Required(), mcplib.Description("Unique ID of the certificate to retrieve.")),
			mcplib.WithString("include", mcplib.Description("A list of additional fields to include in the response. Currently the only supported value is `content` to fetch the...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/certificates/{certificate_id}", []mcpParamBinding{{PublicName: "certificate_id", WireName: "certificate_id", Location: "path"}, {PublicName: "include", WireName: "include", Location: "query"}}, []string{"certificate_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_invite-user",
			mcplib.WithDescription("Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization. Required: email, role. Optional: projects. Returns the new Invite."),
			mcplib.WithString("email", mcplib.Required(), mcplib.Description("Send an email to this address")),
			mcplib.WithString("projects", mcplib.Description("An array of projects to which membership is granted at the same time the org invite is accepted. If omitted, the...")),
			mcplib.WithString("role", mcplib.Required(), mcplib.Description("`owner` or `reader`")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/invites", []mcpParamBinding{{PublicName: "email", WireName: "email", Location: "body"}, {PublicName: "projects", WireName: "projects", Location: "body"}, {PublicName: "role", WireName: "role", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-audit-logs",
			mcplib.WithDescription("List user actions and configuration changes within this organization. Optional: effective_at, project_ids[], event_types[] (plus 6 more). Returns array of AuditLog."),
			mcplib.WithString("effective_at", mcplib.Description("Return only events whose `effective_at` (Unix seconds) is in this range.")),
			mcplib.WithString("project_ids[]", mcplib.Description("Return only events for these projects.")),
			mcplib.WithString("event_types[]", mcplib.Description("Return only events with a `type` in one of these values. For example, `project.created`. For all options, see the...")),
			mcplib.WithString("actor_ids[]", mcplib.Description("Return only events performed by these actors. Can be a user ID, a service account ID, or an api key tracking ID.")),
			mcplib.WithString("actor_emails[]", mcplib.Description("Return only events performed by users with these emails.")),
			mcplib.WithString("resource_ids[]", mcplib.Description("Return only events performed on these targets. For example, a project ID updated.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/audit_logs", []mcpParamBinding{{PublicName: "effective_at", WireName: "effective_at", Location: "query"}, {PublicName: "project_ids[]", WireName: "project_ids[]", Location: "query"}, {PublicName: "event_types[]", WireName: "event_types[]", Location: "query"}, {PublicName: "actor_ids[]", WireName: "actor_ids[]", Location: "query"}, {PublicName: "actor_emails[]", WireName: "actor_emails[]", Location: "query"}, {PublicName: "resource_ids[]", WireName: "resource_ids[]", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-certificates",
			mcplib.WithDescription("List uploaded certificates for this organization. Optional: limit (default: 20), after, order (default: desc). Returns array of OrganizationCertificate."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/certificates", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-group-role-assignments",
			mcplib.WithDescription("Lists the organization roles assigned to a group within the organization. Required: group_id. Optional: limit, after, order. Returns array of AssignedRoleDetails."),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group whose organization role assignments you want to list.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of organization role assignments to return.")),
			mcplib.WithString("after", mcplib.Description("Cursor for pagination. Provide the value from the previous response's `next` field to continue listing organization...")),
			mcplib.WithString("order", mcplib.Description("Sort order for the returned organization roles.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/groups/{group_id}/roles", []mcpParamBinding{{PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-group-users",
			mcplib.WithDescription("Lists the users assigned to a group. Required: group_id. Optional: limit (default: 100), after, order (default: desc). Returns array of GroupUser."),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group to inspect.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of users to be returned. Limit can range between 0 and 1000, and the default is 100.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination. Provide the ID of the last user from the previous list response to retrieve the next...")),
			mcplib.WithString("order", mcplib.Description("Specifies the sort order of users in the list.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/groups/{group_id}/users", []mcpParamBinding{{PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-groups",
			mcplib.WithDescription("Lists all groups in the organization. Optional: limit (default: 100), after, order (default: asc). Returns array of GroupResponse."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of groups to be returned. Limit can range between 0 and 1000, and the default is 100.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination. `after` is a group ID that defines your place in the list. For instance, if you make...")),
			mcplib.WithString("order", mcplib.Description("Specifies the sort order of the returned groups.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/groups", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-invites",
			mcplib.WithDescription("Returns a list of invites in the organization. Optional: limit (default: 20), after."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/invites", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-project-api-keys",
			mcplib.WithDescription("Returns a list of API keys in the project. Required: project_id. Optional: limit (default: 20), after."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/api_keys", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-project-certificates",
			mcplib.WithDescription("List certificates for this project. Required: project_id. Optional: limit (default: 20), after, order (default: desc). Returns array of OrganizationProjectCertificate."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/certificates", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-project-groups",
			mcplib.WithDescription("Lists the groups that have access to a project. Required: project_id. Optional: limit (default: 20), after, order (default: asc). Returns array of ProjectGroup."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to inspect.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of project groups to return. Defaults to 20.")),
			mcplib.WithString("after", mcplib.Description("Cursor for pagination. Provide the ID of the last group from the previous response to fetch the next page.")),
			mcplib.WithString("order", mcplib.Description("Sort order for the returned groups.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/groups", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-project-rate-limits",
			mcplib.WithDescription("Returns the rate limits per model for a project. Required: project_id. Optional: limit (default: 100), after, before."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned. The default is 100.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/rate_limits", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-project-service-accounts",
			mcplib.WithDescription("Returns a list of service accounts in the project. Required: project_id. Optional: limit (default: 20), after."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/service_accounts", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-project-users",
			mcplib.WithDescription("Returns a list of users in the project. Required: project_id. Optional: limit (default: 20), after."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/users", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-projects",
			mcplib.WithDescription("Returns a list of projects. Optional: limit (default: 20), after, include_archived (default: false)."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithBoolean("include_archived", mcplib.Description("If `true` returns all projects including those that have been `archived`. Archived projects are not included by default.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "include_archived", WireName: "include_archived", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-roles",
			mcplib.WithDescription("Lists the roles configured for the organization. Optional: limit (default: 1000), after, order (default: asc). Returns array of Role."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of roles to return. Defaults to 1000.")),
			mcplib.WithString("after", mcplib.Description("Cursor for pagination. Provide the value from the previous response's `next` field to continue listing roles.")),
			mcplib.WithString("order", mcplib.Description("Sort order for the returned roles.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/roles", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-user-role-assignments",
			mcplib.WithDescription("Lists the organization roles assigned to a user within the organization. Required: user_id. Optional: limit, after, order. Returns array of AssignedRoleDetails."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user to inspect.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of organization role assignments to return.")),
			mcplib.WithString("after", mcplib.Description("Cursor for pagination. Provide the value from the previous response's `next` field to continue listing organization...")),
			mcplib.WithString("order", mcplib.Description("Sort order for the returned organization roles.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/users/{user_id}/roles", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_list-users",
			mcplib.WithDescription("Lists all of the users in the organization. Optional: limit (default: 20), after, emails. Returns array of User."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("emails", mcplib.Description("Filter by the email address of users.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/users", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "emails", WireName: "emails", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_modify-certificate",
			mcplib.WithDescription("Modify a certificate. Note that only the name can be modified. Required: certificate_id. Optional: name. Returns the new Certificate."),
			mcplib.WithString("certificate_id", mcplib.Required(), mcplib.Description("Unique ID of the certificate to modify.")),
			mcplib.WithString("name", mcplib.Description("The updated name for the certificate")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/certificates/{certificate_id}", []mcpParamBinding{{PublicName: "certificate_id", WireName: "certificate_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"certificate_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_modify-project",
			mcplib.WithDescription("Modifies a project in the organization. Required: project_id. Optional: external_key_id, geography, name. Returns the new Project."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("external_key_id", mcplib.Description("External key ID to associate with the project.")),
			mcplib.WithString("geography", mcplib.Description("Geography for the project.")),
			mcplib.WithString("name", mcplib.Description("The updated name of the project, this name appears in reports.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "external_key_id", WireName: "external_key_id", Location: "body"}, {PublicName: "geography", WireName: "geography", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_modify-project-user",
			mcplib.WithDescription("Modifies a user's role in the project. Required: project_id, user_id. Optional: role. Returns the new ProjectUser."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user.")),
			mcplib.WithString("role", mcplib.Description("`owner` or `member`")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}/users/{user_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "role", WireName: "role", Location: "body"}}, []string{"project_id", "user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_modify-user",
			mcplib.WithDescription("Modifies a user's role in the organization. Required: user_id. Optional: developer_persona, role, role_id (plus 1 more). Returns the new User."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user.")),
			mcplib.WithString("developer_persona", mcplib.Description("Developer persona metadata.")),
			mcplib.WithString("role", mcplib.Description("`owner` or `reader`")),
			mcplib.WithString("role_id", mcplib.Description("Role ID to assign to the user.")),
			mcplib.WithString("technical_level", mcplib.Description("Technical level metadata.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/users/{user_id}", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "developer_persona", WireName: "developer_persona", Location: "body"}, {PublicName: "role", WireName: "role", Location: "body"}, {PublicName: "role_id", WireName: "role_id", Location: "body"}, {PublicName: "technical_level", WireName: "technical_level", Location: "body"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_remove-group-user",
			mcplib.WithDescription("Removes a user from a group. Required: group_id, user_id. Returns the GroupUserDeletedResource. Destructive."),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group to update.")),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user to remove from the group.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/groups/{group_id}/users/{user_id}", []mcpParamBinding{{PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"group_id", "user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_remove-project-group",
			mcplib.WithDescription("Revokes a group's access to a project. Required: project_id, group_id. Returns the ProjectGroupDeletedResource. Destructive."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to update.")),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group to remove from the project.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/projects/{project_id}/groups/{group_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "group_id", WireName: "group_id", Location: "path"}}, []string{"project_id", "group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_retrieve-invite",
			mcplib.WithDescription("Retrieves an invite. Required: invite_id. Returns the Invite."),
			mcplib.WithString("invite_id", mcplib.Required(), mcplib.Description("The ID of the invite to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/invites/{invite_id}", []mcpParamBinding{{PublicName: "invite_id", WireName: "invite_id", Location: "path"}}, []string{"invite_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_retrieve-project",
			mcplib.WithDescription("Retrieves a project. Required: project_id. Returns the Project."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_retrieve-project-api-key",
			mcplib.WithDescription("Retrieves an API key in the project. Required: project_id, api_key_id. Returns the ProjectApiKey."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("api_key_id", mcplib.Required(), mcplib.Description("The ID of the API key.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/api_keys/{api_key_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "api_key_id", WireName: "api_key_id", Location: "path"}}, []string{"project_id", "api_key_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_retrieve-project-service-account",
			mcplib.WithDescription("Retrieves a service account in the project. Required: project_id, service_account_id. Returns the ProjectServiceAccount."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("service_account_id", mcplib.Required(), mcplib.Description("The ID of the service account.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/service_accounts/{service_account_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "service_account_id", WireName: "service_account_id", Location: "path"}}, []string{"project_id", "service_account_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_retrieve-project-user",
			mcplib.WithDescription("Retrieves a user in the project. Required: project_id, user_id. Returns the ProjectUser."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/projects/{project_id}/users/{user_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"project_id", "user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_retrieve-user",
			mcplib.WithDescription("Retrieves a user by their identifier. Required: user_id. Returns the User."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/users/{user_id}", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_unassign-group-role",
			mcplib.WithDescription("Unassigns an organization role from a group within the organization. Required: group_id, role_id. Returns the DeletedRoleAssignmentResource. Destructive."),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group to modify.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("The ID of the organization role to remove from the group.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/groups/{group_id}/roles/{role_id}", []mcpParamBinding{{PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "path"}}, []string{"group_id", "role_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_unassign-user-role",
			mcplib.WithDescription("Unassigns an organization role from a user within the organization. Required: user_id, role_id. Returns the DeletedRoleAssignmentResource. Destructive."),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user to modify.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("The ID of the organization role to remove from the user.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/organization/users/{user_id}/roles/{role_id}", []mcpParamBinding{{PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "path"}}, []string{"user_id", "role_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_update-group",
			mcplib.WithDescription("Updates a group's information. Required: group_id, name. Returns the new GroupResourceWithSuccess."),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group to update.")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("New display name for the group.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/groups/{group_id}", []mcpParamBinding{{PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_update-project-rate-limits",
			mcplib.WithDescription("Updates a project rate limit. Required: project_id, rate_limit_id. Optional: batch_1_day_max_input_tokens, max_audio_megabytes_per_1_minute, max_images_per_1_minute (plus 3 more). Returns the new ProjectRateLimit."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project.")),
			mcplib.WithString("rate_limit_id", mcplib.Required(), mcplib.Description("The ID of the rate limit.")),
			mcplib.WithNumber("batch_1_day_max_input_tokens", mcplib.Description("The maximum batch input tokens per day. Only relevant for certain models.")),
			mcplib.WithNumber("max_audio_megabytes_per_1_minute", mcplib.Description("The maximum audio megabytes per minute. Only relevant for certain models.")),
			mcplib.WithNumber("max_images_per_1_minute", mcplib.Description("The maximum images per minute. Only relevant for certain models.")),
			mcplib.WithNumber("max_requests_per_1_day", mcplib.Description("The maximum requests per day. Only relevant for certain models.")),
			mcplib.WithNumber("max_requests_per_1_minute", mcplib.Description("The maximum requests per minute.")),
			mcplib.WithNumber("max_tokens_per_1_minute", mcplib.Description("The maximum tokens per minute.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/projects/{project_id}/rate_limits/{rate_limit_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "rate_limit_id", WireName: "rate_limit_id", Location: "path"}, {PublicName: "batch_1_day_max_input_tokens", WireName: "batch_1_day_max_input_tokens", Location: "body"}, {PublicName: "max_audio_megabytes_per_1_minute", WireName: "max_audio_megabytes_per_1_minute", Location: "body"}, {PublicName: "max_images_per_1_minute", WireName: "max_images_per_1_minute", Location: "body"}, {PublicName: "max_requests_per_1_day", WireName: "max_requests_per_1_day", Location: "body"}, {PublicName: "max_requests_per_1_minute", WireName: "max_requests_per_1_minute", Location: "body"}, {PublicName: "max_tokens_per_1_minute", WireName: "max_tokens_per_1_minute", Location: "body"}}, []string{"project_id", "rate_limit_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_update-role",
			mcplib.WithDescription("Updates an existing organization role. Required: role_id. Optional: description, permissions, role_name. Returns the new Role."),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("The ID of the role to update.")),
			mcplib.WithString("description", mcplib.Description("New description for the role.")),
			mcplib.WithString("permissions", mcplib.Description("Updated set of permissions for the role.")),
			mcplib.WithString("role_name", mcplib.Description("New name for the role.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/roles/{role_id}", []mcpParamBinding{{PublicName: "role_id", WireName: "role_id", Location: "path"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "permissions", WireName: "permissions", Location: "body"}, {PublicName: "role_name", WireName: "role_name", Location: "body"}}, []string{"role_id"}),
	)
	s.AddTool(
		mcplib.NewTool("organization_upload-certificate",
			mcplib.WithDescription("Upload a certificate to the organization. This does **not** automatically activate the certificate. Organizations can upload up to 50 certificates. Required: certificate. Optional: name. Returns the new Certificate."),
			mcplib.WithString("certificate", mcplib.Required(), mcplib.Description("The certificate content in PEM format")),
			mcplib.WithString("name", mcplib.Description("An optional name for the certificate")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/organization/certificates", []mcpParamBinding{{PublicName: "certificate", WireName: "certificate", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-audio-speeches",
			mcplib.WithDescription("Get audio speeches usage details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), project_ids (plus 6 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response.")),
			mcplib.WithString("project_ids", mcplib.Description("Return only usage for these projects.")),
			mcplib.WithString("user_ids", mcplib.Description("Return only usage for these users.")),
			mcplib.WithString("api_key_ids", mcplib.Description("Return only usage for these API keys.")),
			mcplib.WithString("models", mcplib.Description("Return only usage for these models.")),
			mcplib.WithString("group_by", mcplib.Description("Group the usage data by the specified fields.")),
			mcplib.WithNumber("limit", mcplib.Description("Specifies the number of buckets to return.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/usage/audio_speeches", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "user_ids", WireName: "user_ids", Location: "query"}, {PublicName: "api_key_ids", WireName: "api_key_ids", Location: "query"}, {PublicName: "models", WireName: "models", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-audio-transcriptions",
			mcplib.WithDescription("Get audio transcriptions usage details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), project_ids (plus 6 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response.")),
			mcplib.WithString("project_ids", mcplib.Description("Return only usage for these projects.")),
			mcplib.WithString("user_ids", mcplib.Description("Return only usage for these users.")),
			mcplib.WithString("api_key_ids", mcplib.Description("Return only usage for these API keys.")),
			mcplib.WithString("models", mcplib.Description("Return only usage for these models.")),
			mcplib.WithString("group_by", mcplib.Description("Group the usage data by the specified fields.")),
			mcplib.WithNumber("limit", mcplib.Description("Specifies the number of buckets to return.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/usage/audio_transcriptions", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "user_ids", WireName: "user_ids", Location: "query"}, {PublicName: "api_key_ids", WireName: "api_key_ids", Location: "query"}, {PublicName: "models", WireName: "models", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-code-interpreter-sessions",
			mcplib.WithDescription("Get code interpreter sessions usage details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), project_ids (plus 3 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response.")),
			mcplib.WithString("project_ids", mcplib.Description("Return only usage for these projects.")),
			mcplib.WithString("group_by", mcplib.Description("Group the usage data by the specified fields. Support fields include `project_id`.")),
			mcplib.WithNumber("limit", mcplib.Description("Specifies the number of buckets to return.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/usage/code_interpreter_sessions", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-completions",
			mcplib.WithDescription("Get completions usage details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), project_ids (plus 7 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response.")),
			mcplib.WithString("project_ids", mcplib.Description("Return only usage for these projects.")),
			mcplib.WithString("user_ids", mcplib.Description("Return only usage for these users.")),
			mcplib.WithString("api_key_ids", mcplib.Description("Return only usage for these API keys.")),
			mcplib.WithString("models", mcplib.Description("Return only usage for these models.")),
			mcplib.WithBoolean("batch", mcplib.Description("If `true`, return batch jobs only. If `false`, return non-batch jobs only. By default, return both.")),
			mcplib.WithString("group_by", mcplib.Description("Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`,...")),
			mcplib.WithNumber("limit", mcplib.Description("Specifies the number of buckets to return.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/usage/completions", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "user_ids", WireName: "user_ids", Location: "query"}, {PublicName: "api_key_ids", WireName: "api_key_ids", Location: "query"}, {PublicName: "models", WireName: "models", Location: "query"}, {PublicName: "batch", WireName: "batch", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-costs",
			mcplib.WithDescription("Get costs details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), project_ids (plus 4 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response. Currently only `1d` is supported, default to `1d`.")),
			mcplib.WithString("project_ids", mcplib.Description("Return only costs for these projects.")),
			mcplib.WithString("api_key_ids", mcplib.Description("Return only costs for these API keys.")),
			mcplib.WithString("group_by", mcplib.Description("Group the costs by the specified fields. Support fields include `project_id`, `line_item`, `api_key_id` and any...")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of buckets to be returned. Limit can range between 1 and 180, and the default is 7.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/costs", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "api_key_ids", WireName: "api_key_ids", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-embeddings",
			mcplib.WithDescription("Get embeddings usage details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), project_ids (plus 6 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response.")),
			mcplib.WithString("project_ids", mcplib.Description("Return only usage for these projects.")),
			mcplib.WithString("user_ids", mcplib.Description("Return only usage for these users.")),
			mcplib.WithString("api_key_ids", mcplib.Description("Return only usage for these API keys.")),
			mcplib.WithString("models", mcplib.Description("Return only usage for these models.")),
			mcplib.WithString("group_by", mcplib.Description("Group the usage data by the specified fields.")),
			mcplib.WithNumber("limit", mcplib.Description("Specifies the number of buckets to return.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/usage/embeddings", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "user_ids", WireName: "user_ids", Location: "query"}, {PublicName: "api_key_ids", WireName: "api_key_ids", Location: "query"}, {PublicName: "models", WireName: "models", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-images",
			mcplib.WithDescription("Get images usage details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), sources (plus 8 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response.")),
			mcplib.WithString("sources", mcplib.Description("Return only usages for these sources. Possible values are `image.generation`, `image.edit`, `image.variation` or any...")),
			mcplib.WithString("sizes", mcplib.Description("Return only usages for these image sizes. Possible values are `256x256`, `512x512`, `1024x1024`, `1792x1792`,...")),
			mcplib.WithString("project_ids", mcplib.Description("Return only usage for these projects.")),
			mcplib.WithString("user_ids", mcplib.Description("Return only usage for these users.")),
			mcplib.WithString("api_key_ids", mcplib.Description("Return only usage for these API keys.")),
			mcplib.WithString("models", mcplib.Description("Return only usage for these models.")),
			mcplib.WithString("group_by", mcplib.Description("Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`,...")),
			mcplib.WithNumber("limit", mcplib.Description("Specifies the number of buckets to return.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/usage/images", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "sources", WireName: "sources", Location: "query"}, {PublicName: "sizes", WireName: "sizes", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "user_ids", WireName: "user_ids", Location: "query"}, {PublicName: "api_key_ids", WireName: "api_key_ids", Location: "query"}, {PublicName: "models", WireName: "models", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-moderations",
			mcplib.WithDescription("Get moderations usage details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), project_ids (plus 6 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response.")),
			mcplib.WithString("project_ids", mcplib.Description("Return only usage for these projects.")),
			mcplib.WithString("user_ids", mcplib.Description("Return only usage for these users.")),
			mcplib.WithString("api_key_ids", mcplib.Description("Return only usage for these API keys.")),
			mcplib.WithString("models", mcplib.Description("Return only usage for these models.")),
			mcplib.WithString("group_by", mcplib.Description("Group the usage data by the specified fields.")),
			mcplib.WithNumber("limit", mcplib.Description("Specifies the number of buckets to return.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/usage/moderations", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "user_ids", WireName: "user_ids", Location: "query"}, {PublicName: "api_key_ids", WireName: "api_key_ids", Location: "query"}, {PublicName: "models", WireName: "models", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("organization_usage-vector-stores",
			mcplib.WithDescription("Get vector stores usage details for the organization. Required: start_time. Optional: end_time, bucket_width (default: 1d), project_ids (plus 3 more). Returns array of UsageTimeBucket."),
			mcplib.WithNumber("start_time", mcplib.Required(), mcplib.Description("Start time (Unix seconds) of the query time range, inclusive.")),
			mcplib.WithNumber("end_time", mcplib.Description("End time (Unix seconds) of the query time range, exclusive.")),
			mcplib.WithString("bucket_width", mcplib.Description("Width of each time bucket in response.")),
			mcplib.WithString("project_ids", mcplib.Description("Return only usage for these projects.")),
			mcplib.WithString("group_by", mcplib.Description("Group the usage data by the specified fields. Support fields include `project_id`.")),
			mcplib.WithNumber("limit", mcplib.Description("Specifies the number of buckets to return.")),
			mcplib.WithString("page", mcplib.Description("A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/organization/usage/vector_stores", []mcpParamBinding{{PublicName: "start_time", WireName: "start_time", Location: "query"}, {PublicName: "end_time", WireName: "end_time", Location: "query"}, {PublicName: "bucket_width", WireName: "bucket_width", Location: "query"}, {PublicName: "project_ids", WireName: "project_ids", Location: "query"}, {PublicName: "group_by", WireName: "group_by", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("projects_groups_assign-project-role",
			mcplib.WithDescription("Assigns a project role to a group within a project. Required: project_id, group_id, role_id. Returns the new GroupRoleAssignment."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to update.")),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group that should receive the project role.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("Identifier of the role to assign.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/projects/{project_id}/groups/{group_id}/roles", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "body"}}, []string{"project_id", "group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_groups_list-project-role-assignments",
			mcplib.WithDescription("Lists the project roles assigned to a group within a project. Required: project_id, group_id. Optional: limit, after, order. Returns array of AssignedRoleDetails."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to inspect.")),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group to inspect.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of project role assignments to return.")),
			mcplib.WithString("after", mcplib.Description("Cursor for pagination. Provide the value from the previous response's `next` field to continue listing project roles.")),
			mcplib.WithString("order", mcplib.Description("Sort order for the returned project roles.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/projects/{project_id}/groups/{group_id}/roles", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"project_id", "group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_groups_unassign-project-role",
			mcplib.WithDescription("Unassigns a project role from a group within a project. Required: project_id, group_id, role_id. Returns the DeletedRoleAssignmentResource. Destructive."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to modify.")),
			mcplib.WithString("group_id", mcplib.Required(), mcplib.Description("The ID of the group whose project role assignment should be removed.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("The ID of the project role to remove from the group.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/projects/{project_id}/groups/{group_id}/roles/{role_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "group_id", WireName: "group_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "path"}}, []string{"project_id", "group_id", "role_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_roles_create-project",
			mcplib.WithDescription("Creates a custom role for a project. Required: project_id, permissions, role_name. Optional: description. Returns the new Role."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to update.")),
			mcplib.WithString("description", mcplib.Description("Optional description of the role.")),
			mcplib.WithString("permissions", mcplib.Required(), mcplib.Description("Permissions to grant to the role.")),
			mcplib.WithString("role_name", mcplib.Required(), mcplib.Description("Unique name for the role.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/projects/{project_id}/roles", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "permissions", WireName: "permissions", Location: "body"}, {PublicName: "role_name", WireName: "role_name", Location: "body"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_roles_delete-project",
			mcplib.WithDescription("Deletes a custom role from a project. Required: project_id, role_id. Returns the RoleDeletedResource. Destructive."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to update.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("The ID of the role to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/projects/{project_id}/roles/{role_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "path"}}, []string{"project_id", "role_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_roles_list-project",
			mcplib.WithDescription("Lists the roles configured for a project. Required: project_id. Optional: limit (default: 1000), after, order (default: asc). Returns array of Role."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to inspect.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of roles to return. Defaults to 1000.")),
			mcplib.WithString("after", mcplib.Description("Cursor for pagination. Provide the value from the previous response's `next` field to continue listing roles.")),
			mcplib.WithString("order", mcplib.Description("Sort order for the returned roles.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/projects/{project_id}/roles", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"project_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_roles_update-project",
			mcplib.WithDescription("Updates an existing project role. Required: project_id, role_id. Optional: description, permissions, role_name. Returns the new Role."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to update.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("The ID of the role to update.")),
			mcplib.WithString("description", mcplib.Description("New description for the role.")),
			mcplib.WithString("permissions", mcplib.Description("Updated set of permissions for the role.")),
			mcplib.WithString("role_name", mcplib.Description("New name for the role.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/projects/{project_id}/roles/{role_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "path"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "permissions", WireName: "permissions", Location: "body"}, {PublicName: "role_name", WireName: "role_name", Location: "body"}}, []string{"project_id", "role_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_users_assign-project-role",
			mcplib.WithDescription("Assigns a project role to a user within a project. Required: project_id, user_id, role_id. Returns the new UserRoleAssignment."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to update.")),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user that should receive the project role.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("Identifier of the role to assign.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/projects/{project_id}/users/{user_id}/roles", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "body"}}, []string{"project_id", "user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_users_list-project-role-assignments",
			mcplib.WithDescription("Lists the project roles assigned to a user within a project. Required: project_id, user_id. Optional: limit, after, order. Returns array of AssignedRoleDetails."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to inspect.")),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user to inspect.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of project role assignments to return.")),
			mcplib.WithString("after", mcplib.Description("Cursor for pagination. Provide the value from the previous response's `next` field to continue listing project roles.")),
			mcplib.WithString("order", mcplib.Description("Sort order for the returned project roles.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/projects/{project_id}/users/{user_id}/roles", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}}, []string{"project_id", "user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("projects_users_unassign-project-role",
			mcplib.WithDescription("Unassigns a project role from a user within a project. Required: project_id, user_id, role_id. Returns the DeletedRoleAssignmentResource. Destructive."),
			mcplib.WithString("project_id", mcplib.Required(), mcplib.Description("The ID of the project to modify.")),
			mcplib.WithString("user_id", mcplib.Required(), mcplib.Description("The ID of the user whose project role assignment should be removed.")),
			mcplib.WithString("role_id", mcplib.Required(), mcplib.Description("The ID of the project role to remove from the user.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/projects/{project_id}/users/{user_id}/roles/{role_id}", []mcpParamBinding{{PublicName: "project_id", WireName: "project_id", Location: "path"}, {PublicName: "user_id", WireName: "user_id", Location: "path"}, {PublicName: "role_id", WireName: "role_id", Location: "path"}}, []string{"project_id", "user_id", "role_id"}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_accept-call",
			mcplib.WithDescription("Accept an incoming SIP call and configure the realtime session that will handle it. Required: call_id, type. Optional: audio, include, instructions (plus 10 more)."),
			mcplib.WithString("call_id", mcplib.Required(), mcplib.Description("The identifier for the call provided in the [`realtime.")),
			mcplib.WithString("audio", mcplib.Description("Configuration for input and output audio.")),
			mcplib.WithString("include", mcplib.Description("Additional fields to include in server outputs. `item.input_audio_transcription.logprobs`: Include logprobs for...")),
			mcplib.WithString("instructions", mcplib.Description("The default system instructions (i.e. system message) prepended to model calls. This field allows the client to...")),
			mcplib.WithString("max_output_tokens", mcplib.Description("Maximum number of output tokens for a single assistant response, inclusive of tool calls. Provide an integer between...")),
			mcplib.WithString("model", mcplib.Description("The Realtime model used for this session.")),
			mcplib.WithString("output_modalities", mcplib.Description("The set of modalities the model can respond with. It defaults to `['audio']`, indicating that the model will respond...")),
			mcplib.WithBoolean("parallel_tool_calls", mcplib.Description("Whether the model may call multiple tools in parallel. Only supported by reasoning Realtime models such as...")),
			mcplib.WithString("prompt", mcplib.Description("Prompt")),
			mcplib.WithString("reasoning", mcplib.Description("Configuration for reasoning-capable Realtime models such as `gpt-realtime-2`.")),
			mcplib.WithString("tool_choice", mcplib.Description("How the model chooses tools. Provide one of the string modes or force a specific function/MCP tool.")),
			mcplib.WithString("tools", mcplib.Description("Tools available to the model.")),
			mcplib.WithString("tracing", mcplib.Description("Realtime API can write session traces to the [Traces Dashboard](https://platform.openai.com/logs?api=traces). Set to...")),
			mcplib.WithString("truncation", mcplib.Description("When the number of tokens in a conversation exceeds the model's input token limit, the conversation be truncated,...")),
			mcplib.WithString("type", mcplib.Required(), mcplib.Description("The type of session to create. Always `realtime` for the Realtime API.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/calls/{call_id}/accept", []mcpParamBinding{{PublicName: "call_id", WireName: "call_id", Location: "path"}, {PublicName: "audio", WireName: "audio", Location: "body"}, {PublicName: "include", WireName: "include", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "max_output_tokens", WireName: "max_output_tokens", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "output_modalities", WireName: "output_modalities", Location: "body"}, {PublicName: "parallel_tool_calls", WireName: "parallel_tool_calls", Location: "body"}, {PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "reasoning", WireName: "reasoning", Location: "body"}, {PublicName: "tool_choice", WireName: "tool_choice", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "tracing", WireName: "tracing", Location: "body"}, {PublicName: "truncation", WireName: "truncation", Location: "body"}, {PublicName: "type", WireName: "type", Location: "body"}}, []string{"call_id"}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_create-call",
			mcplib.WithDescription("Create a new Realtime API call over WebRTC and receive the SDP answer needed to complete the peer connection. Required: sdp. Optional: session."),
			mcplib.WithString("sdp", mcplib.Required(), mcplib.Description("WebRTC Session Description Protocol (SDP) offer generated by the caller.")),
			mcplib.WithString("session", mcplib.Description("Optional session configuration to apply before the realtime session is created. Use the same parameters you would...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/calls", []mcpParamBinding{{PublicName: "sdp", WireName: "sdp", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "session", WireName: "session", Location: "body", RequestContentType: "multipart/form-data"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_create-client-secret",
			mcplib.WithDescription("Create a Realtime client secret with an associated session configuration. Client secrets are short-lived tokens that can be passed to a client app, such as a web frontend or mobile client, which grants access to the Realtime API without leaking your main API key. You can configure a custom TTL for each client secret. You can also attach session configuration options to the client secret, which will be applied to any sessions created using that client secret, but these can also be overridden by the client connection. [Learn more about authentication with client secrets over WebRTC](/docs/guides/realtime-webrtc). Returns the created client secret and the effective session object. The client secret is a string that looks like `ek_1234`. Optional: expires_after, session."),
			mcplib.WithString("expires_after", mcplib.Description("Configuration for the client secret expiration. Expiration refers to the time after which a client secret will no...")),
			mcplib.WithString("session", mcplib.Description("Session configuration to use for the client secret. Choose either a realtime session or a transcription session.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/client_secrets", []mcpParamBinding{{PublicName: "expires_after", WireName: "expires_after", Location: "body"}, {PublicName: "session", WireName: "session", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_create-session",
			mcplib.WithDescription("Create an ephemeral API token for use in client-side applications with the Realtime API. Can be configured with the same session parameters as the `session.update` client event. It responds with a session object, plus a `client_secret` key which contains a usable ephemeral API token that can be used to authenticate browser clients for the Realtime API. Returns the created Realtime session object, plus an ephemeral key. Required: client_secret. Optional: input_audio_format, input_audio_transcription, instructions (plus 12 more)."),
			mcplib.WithString("client_secret", mcplib.Required(), mcplib.Description("Ephemeral key returned by the API.")),
			mcplib.WithString("input_audio_format", mcplib.Description("The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.")),
			mcplib.WithString("input_audio_transcription", mcplib.Description("Configuration for input audio transcription, defaults to off and can be set to `null` to turn off once on. Input...")),
			mcplib.WithString("instructions", mcplib.Description("The default system instructions (i.e. system message) prepended to model calls. This field allows the client to...")),
			mcplib.WithString("max_response_output_tokens", mcplib.Description("Maximum number of output tokens for a single assistant response, inclusive of tool calls. Provide an integer between...")),
			mcplib.WithString("modalities", mcplib.Description("The set of modalities the model can respond with. To disable audio, set this to ['text'].")),
			mcplib.WithString("output_audio_format", mcplib.Description("The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.")),
			mcplib.WithString("prompt", mcplib.Description("Prompt")),
			mcplib.WithNumber("speed", mcplib.Description("The speed of the model's spoken response. 1.0 is the default speed. 0.25 is the minimum speed. 1.5 is the maximum...")),
			mcplib.WithNumber("temperature", mcplib.Description("Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8.")),
			mcplib.WithString("tool_choice", mcplib.Description("How the model chooses tools. Options are `auto`, `none`, `required`, or specify a function.")),
			mcplib.WithString("tools", mcplib.Description("Tools (functions) available to the model.")),
			mcplib.WithString("tracing", mcplib.Description("Configuration options for tracing. Set to null to disable tracing. Once tracing is enabled for a session, the...")),
			mcplib.WithString("truncation", mcplib.Description("When the number of tokens in a conversation exceeds the model's input token limit, the conversation be truncated,...")),
			mcplib.WithString("turn_detection", mcplib.Description("Configuration for turn detection. Can be set to `null` to turn off. Server VAD means that the model will detect the...")),
			mcplib.WithString("voice", mcplib.Description("The voice the model uses to respond. Supported built-in voices are `alloy`, `ash`, `ballad`, `coral`, `echo`,...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/sessions", []mcpParamBinding{{PublicName: "client_secret", WireName: "client_secret", Location: "body"}, {PublicName: "input_audio_format", WireName: "input_audio_format", Location: "body"}, {PublicName: "input_audio_transcription", WireName: "input_audio_transcription", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "max_response_output_tokens", WireName: "max_response_output_tokens", Location: "body"}, {PublicName: "modalities", WireName: "modalities", Location: "body"}, {PublicName: "output_audio_format", WireName: "output_audio_format", Location: "body"}, {PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "speed", WireName: "speed", Location: "body"}, {PublicName: "temperature", WireName: "temperature", Location: "body"}, {PublicName: "tool_choice", WireName: "tool_choice", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "tracing", WireName: "tracing", Location: "body"}, {PublicName: "truncation", WireName: "truncation", Location: "body"}, {PublicName: "turn_detection", WireName: "turn_detection", Location: "body"}, {PublicName: "voice", WireName: "voice", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_create-transcription-session",
			mcplib.WithDescription("Create an ephemeral API token for use in client-side applications with the Realtime API specifically for realtime transcriptions. Can be configured with the same session parameters as the `transcription_session.update` client event. It responds with a session object, plus a `client_secret` key which contains a usable ephemeral API token that can be used to authenticate browser clients for the Realtime API. Returns the created Realtime transcription session object, plus an ephemeral key. Optional: include, input_audio_format (default: pcm16), input_audio_noise_reduction (plus 2 more)."),
			mcplib.WithString("include", mcplib.Description("The set of items to include in the transcription. Current available items are: `item.input_audio_transcription.logprobs`")),
			mcplib.WithString("input_audio_format", mcplib.Description("The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`. For `pcm16`, input audio must be 16-bit...")),
			mcplib.WithString("input_audio_noise_reduction", mcplib.Description("Configuration for input audio noise reduction. This can be set to `null` to turn off. Noise reduction filters audio...")),
			mcplib.WithString("input_audio_transcription", mcplib.Description("Configuration for input audio transcription. The client can optionally set the language and prompt for...")),
			mcplib.WithString("turn_detection", mcplib.Description("Configuration for turn detection. Can be set to `null` to turn off. Server VAD means that the model will detect the...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/transcription_sessions", []mcpParamBinding{{PublicName: "include", WireName: "include", Location: "body"}, {PublicName: "input_audio_format", WireName: "input_audio_format", Location: "body"}, {PublicName: "input_audio_noise_reduction", WireName: "input_audio_noise_reduction", Location: "body"}, {PublicName: "input_audio_transcription", WireName: "input_audio_transcription", Location: "body"}, {PublicName: "turn_detection", WireName: "turn_detection", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_create-translation-client-secret",
			mcplib.WithDescription("Create a Realtime translation client secret with an associated translation session configuration. Client secrets are short-lived tokens that can be passed to a client app, such as a web frontend or mobile client, which grants access to the Realtime Translation API without leaking your main API key. You can configure a custom TTL for each client secret. Returns the created client secret and the effective translation session object. The client secret is a string that looks like `ek_1234`. Required: session. Optional: expires_after."),
			mcplib.WithString("expires_after", mcplib.Description("Configuration for the client secret expiration. Expiration refers to the time after which a client secret will no...")),
			mcplib.WithString("session", mcplib.Required(), mcplib.Description("Realtime translation session configuration. Translation sessions stream source audio in and translated audio plus...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/translations/client_secrets", []mcpParamBinding{{PublicName: "expires_after", WireName: "expires_after", Location: "body"}, {PublicName: "session", WireName: "session", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_hangup-call",
			mcplib.WithDescription("End an active Realtime API call, whether it was initiated over SIP or WebRTC. Required: call_id."),
			mcplib.WithString("call_id", mcplib.Required(), mcplib.Description("The identifier for the call. For SIP calls, use the value provided in the...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/calls/{call_id}/hangup", []mcpParamBinding{{PublicName: "call_id", WireName: "call_id", Location: "path"}}, []string{"call_id"}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_refer-call",
			mcplib.WithDescription("Transfer an active SIP call to a new destination using the SIP REFER verb. Required: call_id, target_uri."),
			mcplib.WithString("call_id", mcplib.Required(), mcplib.Description("The identifier for the call provided in the [`realtime.")),
			mcplib.WithString("target_uri", mcplib.Required(), mcplib.Description("URI that should appear in the SIP Refer-To header. Supports values like `tel:+14155550123` or `sip:agent@example.com`.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/calls/{call_id}/refer", []mcpParamBinding{{PublicName: "call_id", WireName: "call_id", Location: "path"}, {PublicName: "target_uri", WireName: "target_uri", Location: "body"}}, []string{"call_id"}),
	)
	s.AddTool(
		mcplib.NewTool("realtime_reject-call",
			mcplib.WithDescription("Decline an incoming SIP call by returning a SIP status code to the caller. Required: call_id. Optional: status_code."),
			mcplib.WithString("call_id", mcplib.Required(), mcplib.Description("The identifier for the call provided in the [`realtime.")),
			mcplib.WithNumber("status_code", mcplib.Description("SIP response code to send back to the caller. Defaults to `603` (Decline) when omitted.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/realtime/calls/{call_id}/reject", []mcpParamBinding{{PublicName: "call_id", WireName: "call_id", Location: "path"}, {PublicName: "status_code", WireName: "status_code", Location: "body"}}, []string{"call_id"}),
	)
	s.AddTool(
		mcplib.NewTool("responses_compactconversation",
			mcplib.WithDescription("Compact a conversation. Returns a compacted response object. Learn when and how to compact long-running conversations in the [conversation state guide](/docs/guides/conversation-state#managing-the-context-window). For ZDR-compatible compaction details, see [Compaction (advanced)](/docs/guides/conversation-state#compaction-advanced). Required: model. Optional: input, instructions, previous_response_id (plus 3 more)."),
			mcplib.WithString("input", mcplib.Description("Input")),
			mcplib.WithString("instructions", mcplib.Description("Instructions")),
			mcplib.WithString("model", mcplib.Required(), mcplib.Description("Model ID used to generate the response, like `gpt-5` or `o3`. OpenAI offers a wide range of models with different...")),
			mcplib.WithString("previous_response_id", mcplib.Description("Previous response id")),
			mcplib.WithString("prompt_cache_key", mcplib.Description("Prompt cache key")),
			mcplib.WithString("prompt_cache_retention", mcplib.Description("Prompt cache retention")),
			mcplib.WithString("service_tier", mcplib.Description("Service tier")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/responses/compact", []mcpParamBinding{{PublicName: "input", WireName: "input", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "previous_response_id", WireName: "previous_response_id", Location: "body"}, {PublicName: "prompt_cache_key", WireName: "prompt_cache_key", Location: "body"}, {PublicName: "prompt_cache_retention", WireName: "prompt_cache_retention", Location: "body"}, {PublicName: "service_tier", WireName: "service_tier", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("responses_create",
			mcplib.WithDescription("Creates a model response. Provide [text](/docs/guides/text) or [image](/docs/guides/images) inputs to generate [text](/docs/guides/text) or [JSON](/docs/guides/structured-outputs) outputs. Have the model call your own [custom code](/docs/guides/function-calling) or use built-in [tools](/docs/guides/tools) like [web search](/docs/guides/tools-web-search) or [file search](/docs/guides/tools-file-search) to use your own data as input for the model's response. Optional: background, context_management, conversation (plus 26 more). Returns the new Response."),
			mcplib.WithString("background", mcplib.Description("Background")),
			mcplib.WithString("context_management", mcplib.Description("Context management")),
			mcplib.WithString("conversation", mcplib.Description("Conversation")),
			mcplib.WithString("include", mcplib.Description("Include")),
			mcplib.WithString("input", mcplib.Description("Text, image, or file inputs to the model, used to generate a response. Learn more: - [Text inputs and...")),
			mcplib.WithString("instructions", mcplib.Description("Instructions")),
			mcplib.WithString("max_output_tokens", mcplib.Description("Max output tokens")),
			mcplib.WithString("max_tool_calls", mcplib.Description("Max tool calls")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("model", mcplib.Description("Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI offers a wide range of models with different...")),
			mcplib.WithString("parallel_tool_calls", mcplib.Description("Parallel tool calls")),
			mcplib.WithString("previous_response_id", mcplib.Description("Previous response id")),
			mcplib.WithString("prompt", mcplib.Description("Prompt")),
			mcplib.WithString("prompt_cache_key", mcplib.Description("Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field....")),
			mcplib.WithString("prompt_cache_retention", mcplib.Description("Prompt cache retention")),
			mcplib.WithString("reasoning", mcplib.Description("Reasoning")),
			mcplib.WithString("safety_identifier", mcplib.Description("A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies. The...")),
			mcplib.WithString("service_tier", mcplib.Description("Service tier")),
			mcplib.WithString("store", mcplib.Description("Store")),
			mcplib.WithString("stream", mcplib.Description("Stream")),
			mcplib.WithString("stream_options", mcplib.Description("Stream options")),
			mcplib.WithString("temperature", mcplib.Description("Temperature")),
			mcplib.WithString("text", mcplib.Description("Configuration options for a text response from the model. Can be plain text or structured JSON data. Learn more: -...")),
			mcplib.WithString("tool_choice", mcplib.Description("How the model should select which tool (or tools) to use when generating a response. See the `tools` parameter to...")),
			mcplib.WithString("tools", mcplib.Description("An array of tools the model may call while generating a response. You can specify which tool to use by setting the...")),
			mcplib.WithNumber("top_logprobs", mcplib.Description("An integer between 0 and 20 specifying the maximum number of most likely tokens to return at each token position,...")),
			mcplib.WithString("top_p", mcplib.Description("Top p")),
			mcplib.WithString("truncation", mcplib.Description("Truncation")),
			mcplib.WithString("user", mcplib.Description("This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/responses", []mcpParamBinding{{PublicName: "background", WireName: "background", Location: "body"}, {PublicName: "context_management", WireName: "context_management", Location: "body"}, {PublicName: "conversation", WireName: "conversation", Location: "body"}, {PublicName: "include", WireName: "include", Location: "body"}, {PublicName: "input", WireName: "input", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "max_output_tokens", WireName: "max_output_tokens", Location: "body"}, {PublicName: "max_tool_calls", WireName: "max_tool_calls", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "parallel_tool_calls", WireName: "parallel_tool_calls", Location: "body"}, {PublicName: "previous_response_id", WireName: "previous_response_id", Location: "body"}, {PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "prompt_cache_key", WireName: "prompt_cache_key", Location: "body"}, {PublicName: "prompt_cache_retention", WireName: "prompt_cache_retention", Location: "body"}, {PublicName: "reasoning", WireName: "reasoning", Location: "body"}, {PublicName: "safety_identifier", WireName: "safety_identifier", Location: "body"}, {PublicName: "service_tier", WireName: "service_tier", Location: "body"}, {PublicName: "store", WireName: "store", Location: "body"}, {PublicName: "stream", WireName: "stream", Location: "body"}, {PublicName: "stream_options", WireName: "stream_options", Location: "body"}, {PublicName: "temperature", WireName: "temperature", Location: "body"}, {PublicName: "text", WireName: "text", Location: "body"}, {PublicName: "tool_choice", WireName: "tool_choice", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "top_logprobs", WireName: "top_logprobs", Location: "body"}, {PublicName: "top_p", WireName: "top_p", Location: "body"}, {PublicName: "truncation", WireName: "truncation", Location: "body"}, {PublicName: "user", WireName: "user", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("responses_delete",
			mcplib.WithDescription("Deletes a model response with the given ID. Required: response_id. Destructive."),
			mcplib.WithString("response_id", mcplib.Required(), mcplib.Description("The ID of the response to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/responses/{response_id}", []mcpParamBinding{{PublicName: "response_id", WireName: "response_id", Location: "path"}}, []string{"response_id"}),
	)
	s.AddTool(
		mcplib.NewTool("responses_get",
			mcplib.WithDescription("Retrieves a model response with the given ID. Required: response_id. Optional: include, stream, starting_after (plus 1 more). Returns the Response."),
			mcplib.WithString("response_id", mcplib.Required(), mcplib.Description("The ID of the response to retrieve.")),
			mcplib.WithString("include", mcplib.Description("Additional fields to include in the response. See the `include` parameter for Response creation above for more...")),
			mcplib.WithBoolean("stream", mcplib.Description("If set to true, the model response data will be streamed to the client as it is generated using [server-sent...")),
			mcplib.WithNumber("starting_after", mcplib.Description("The sequence number of the event after which to start streaming.")),
			mcplib.WithBoolean("include_obfuscation", mcplib.Description("When true, stream obfuscation will be enabled. Stream obfuscation adds random characters to an `obfuscation` field...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/responses/{response_id}", []mcpParamBinding{{PublicName: "response_id", WireName: "response_id", Location: "path"}, {PublicName: "include", WireName: "include", Location: "query"}, {PublicName: "stream", WireName: "stream", Location: "query"}, {PublicName: "starting_after", WireName: "starting_after", Location: "query"}, {PublicName: "include_obfuscation", WireName: "include_obfuscation", Location: "query"}}, []string{"response_id"}),
	)
	s.AddTool(
		mcplib.NewTool("responses_getinputtokencounts",
			mcplib.WithDescription("Returns input token counts of the request. Returns an object with `object` set to `response.input_tokens` and an `input_tokens` count. Optional: conversation, input, instructions (plus 8 more)."),
			mcplib.WithString("conversation", mcplib.Description("Conversation")),
			mcplib.WithString("input", mcplib.Description("Input")),
			mcplib.WithString("instructions", mcplib.Description("Instructions")),
			mcplib.WithString("model", mcplib.Description("Model")),
			mcplib.WithString("parallel_tool_calls", mcplib.Description("Parallel tool calls")),
			mcplib.WithString("previous_response_id", mcplib.Description("Previous response id")),
			mcplib.WithString("reasoning", mcplib.Description("Reasoning")),
			mcplib.WithString("text", mcplib.Description("Text")),
			mcplib.WithString("tool_choice", mcplib.Description("Tool choice")),
			mcplib.WithString("tools", mcplib.Description("Tools")),
			mcplib.WithString("truncation", mcplib.Description("The truncation strategy to use for the model response. - `auto`: If the input to this Response exceeds the model's...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/responses/input_tokens", []mcpParamBinding{{PublicName: "conversation", WireName: "conversation", Location: "body"}, {PublicName: "input", WireName: "input", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "parallel_tool_calls", WireName: "parallel_tool_calls", Location: "body"}, {PublicName: "previous_response_id", WireName: "previous_response_id", Location: "body"}, {PublicName: "reasoning", WireName: "reasoning", Location: "body"}, {PublicName: "text", WireName: "text", Location: "body"}, {PublicName: "tool_choice", WireName: "tool_choice", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "truncation", WireName: "truncation", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("responses_cancel_response",
			mcplib.WithDescription("Cancels a model response with the given ID. Only responses created with the `background` parameter set to `true` can be cancelled. [Learn more](/docs/guides/background). Required: response_id. Returns the new Response."),
			mcplib.WithString("response_id", mcplib.Required(), mcplib.Description("The ID of the response to cancel.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/responses/{response_id}/cancel", []mcpParamBinding{{PublicName: "response_id", WireName: "response_id", Location: "path"}}, []string{"response_id"}),
	)
	s.AddTool(
		mcplib.NewTool("responses_input-items_list",
			mcplib.WithDescription("Returns a list of input items for a given response. Required: response_id. Optional: limit (default: 20), order, after (plus 1 more)."),
			mcplib.WithString("response_id", mcplib.Required(), mcplib.Description("The ID of the response to retrieve input items for.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("The order to return the input items in. Default is `desc`. - `asc`: Return the input items in ascending order. -...")),
			mcplib.WithString("after", mcplib.Description("An item ID to list items after, used in pagination.")),
			mcplib.WithString("include", mcplib.Description("Additional fields to include in the response. See the `include` parameter for Response creation above for more...")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/responses/{response_id}/input_items", []mcpParamBinding{{PublicName: "response_id", WireName: "response_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "include", WireName: "include", Location: "query"}}, []string{"response_id"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_create",
			mcplib.WithDescription("Create a new skill. Required: files. Returns the new SkillResource."),
			mcplib.WithString("files", mcplib.Required(), mcplib.Description("Files")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/skills", []mcpParamBinding{{PublicName: "files", WireName: "files", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("skills_delete",
			mcplib.WithDescription("Delete a skill by its ID. Required: skill_id. Returns the DeletedSkillResource. Destructive."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/skills/{skill_id}", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}}, []string{"skill_id"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_get",
			mcplib.WithDescription("Get a skill by its ID. Required: skill_id. Returns the SkillResource."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/skills/{skill_id}", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}}, []string{"skill_id"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_list",
			mcplib.WithDescription("List all skills for the current project. Optional: limit, order, after. Returns array of SkillResource."),
			mcplib.WithNumber("limit", mcplib.Description("Number of items to retrieve")),
			mcplib.WithString("order", mcplib.Description("Sort order of results by timestamp. Use `asc` for ascending order or `desc` for descending order.")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last item from the previous pagination request")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/skills", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("skills_update-default-version",
			mcplib.WithDescription("Update the default version pointer for a skill. Required: skill_id, default_version. Returns the new SkillResource."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill.")),
			mcplib.WithString("default_version", mcplib.Required(), mcplib.Description("The skill version number to set as default.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/skills/{skill_id}", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}, {PublicName: "default_version", WireName: "default_version", Location: "body"}}, []string{"skill_id"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_content_get-skill",
			mcplib.WithDescription("Download a skill zip bundle by its ID. Required: skill_id."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill to download.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/skills/{skill_id}/content", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}}, []string{"skill_id"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_versions_create-skill",
			mcplib.WithDescription("Create a new immutable skill version. Required: skill_id, files. Optional: default. Returns the new SkillVersionResource."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill to version.")),
			mcplib.WithBoolean("default", mcplib.Description("Whether to set this version as the default.")),
			mcplib.WithString("files", mcplib.Required(), mcplib.Description("Files")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/skills/{skill_id}/versions", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}, {PublicName: "default", WireName: "default", Location: "body"}, {PublicName: "files", WireName: "files", Location: "body"}}, []string{"skill_id"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_versions_delete-skill",
			mcplib.WithDescription("Delete a skill version. Required: skill_id, version. Returns the DeletedSkillVersionResource. Destructive."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill.")),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("The skill version number.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/skills/{skill_id}/versions/{version}", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}, {PublicName: "version", WireName: "version", Location: "path"}}, []string{"skill_id", "version"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_versions_get-skill",
			mcplib.WithDescription("Get a specific skill version. Required: skill_id, version. Returns the SkillVersionResource."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill.")),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("The version number to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/skills/{skill_id}/versions/{version}", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}, {PublicName: "version", WireName: "version", Location: "path"}}, []string{"skill_id", "version"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_versions_get-skill-content",
			mcplib.WithDescription("Download a skill version zip bundle. Required: skill_id, version."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill.")),
			mcplib.WithString("version", mcplib.Required(), mcplib.Description("The skill version number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/skills/{skill_id}/versions/{version}/content", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}, {PublicName: "version", WireName: "version", Location: "path"}}, []string{"skill_id", "version"}),
	)
	s.AddTool(
		mcplib.NewTool("skills_versions_list-skill",
			mcplib.WithDescription("List skill versions for a skill. Required: skill_id. Optional: limit, order, after. Returns array of SkillVersionResource."),
			mcplib.WithString("skill_id", mcplib.Required(), mcplib.Description("The identifier of the skill.")),
			mcplib.WithNumber("limit", mcplib.Description("Number of versions to retrieve.")),
			mcplib.WithString("order", mcplib.Description("Sort order of results by version number.")),
			mcplib.WithString("after", mcplib.Description("The skill version ID to start after.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/skills/{skill_id}/versions", []mcpParamBinding{{PublicName: "skill_id", WireName: "skill_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{"skill_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_create",
			mcplib.WithDescription("Create a thread. Optional: messages, metadata, tool_resources. Returns the new ThreadObject."),
			mcplib.WithString("messages", mcplib.Description("A list of [messages](/docs/api-reference/messages) to start the thread with.")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("tool_resources", mcplib.Description("Tool resources")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads", []mcpParamBinding{{PublicName: "messages", WireName: "messages", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "tool_resources", WireName: "tool_resources", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("threads_create-and-run",
			mcplib.WithDescription("Create a thread and run it in one request. Required: assistant_id. Optional: instructions, max_completion_tokens, max_prompt_tokens (plus 12 more). Returns the new RunObject."),
			mcplib.WithString("assistant_id", mcplib.Required(), mcplib.Description("The ID of the [assistant](/docs/api-reference/assistants) to use to execute this run.")),
			mcplib.WithString("instructions", mcplib.Description("Override the default system message of the assistant. This is useful for modifying the behavior on a per-run basis.")),
			mcplib.WithNumber("max_completion_tokens", mcplib.Description("The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort...")),
			mcplib.WithNumber("max_prompt_tokens", mcplib.Description("The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to...")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("model", mcplib.Description("The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it...")),
			mcplib.WithBoolean("parallel_tool_calls", mcplib.Description("Whether to enable [parallel function...")),
			mcplib.WithString("response_format", mcplib.Description("Specifies the format that the model must output. Compatible with [GPT-4o](/docs/models#gpt-4o), [GPT-4...")),
			mcplib.WithBoolean("stream", mcplib.Description("If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run...")),
			mcplib.WithNumber("temperature", mcplib.Description("What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while...")),
			mcplib.WithString("thread", mcplib.Description("Options to create a new thread. If no thread is provided when running a request, an empty thread will be created.")),
			mcplib.WithString("tool_choice", mcplib.Description("Tool choice")),
			mcplib.WithString("tool_resources", mcplib.Description("A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For...")),
			mcplib.WithString("tools", mcplib.Description("Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.")),
			mcplib.WithNumber("top_p", mcplib.Description("An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the...")),
			mcplib.WithString("truncation_strategy", mcplib.Description("Controls for how a thread will be truncated prior to the run. Use this to control the initial context window of the run.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads/runs", []mcpParamBinding{{PublicName: "assistant_id", WireName: "assistant_id", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "max_completion_tokens", WireName: "max_completion_tokens", Location: "body"}, {PublicName: "max_prompt_tokens", WireName: "max_prompt_tokens", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "parallel_tool_calls", WireName: "parallel_tool_calls", Location: "body"}, {PublicName: "response_format", WireName: "response_format", Location: "body"}, {PublicName: "stream", WireName: "stream", Location: "body"}, {PublicName: "temperature", WireName: "temperature", Location: "body"}, {PublicName: "thread", WireName: "thread", Location: "body"}, {PublicName: "tool_choice", WireName: "tool_choice", Location: "body"}, {PublicName: "tool_resources", WireName: "tool_resources", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "top_p", WireName: "top_p", Location: "body"}, {PublicName: "truncation_strategy", WireName: "truncation_strategy", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("threads_delete",
			mcplib.WithDescription("Delete a thread. Required: thread_id. Returns the DeleteThreadResponse. Destructive."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/threads/{thread_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_get",
			mcplib.WithDescription("Retrieves a thread. Required: thread_id. Returns the ThreadObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/threads/{thread_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_modify",
			mcplib.WithDescription("Modifies a thread. Required: thread_id. Optional: metadata, tool_resources. Returns the new ThreadObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread to modify. Only the `metadata` can be modified.")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("tool_resources", mcplib.Description("Tool resources")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads/{thread_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "tool_resources", WireName: "tool_resources", Location: "body"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_messages_create",
			mcplib.WithDescription("Create a message. Required: thread_id, content, role. Optional: attachments, metadata. Returns the new MessageObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the [thread](/docs/api-reference/threads) to create a message for.")),
			mcplib.WithString("attachments", mcplib.Description("Attachments")),
			mcplib.WithString("content", mcplib.Required(), mcplib.Description("Content")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("role", mcplib.Required(), mcplib.Description("The role of the entity that is creating the message. Allowed values include: - `user`: Indicates the message is sent...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads/{thread_id}/messages", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "attachments", WireName: "attachments", Location: "body"}, {PublicName: "content", WireName: "content", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "role", WireName: "role", Location: "body"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_messages_delete",
			mcplib.WithDescription("Deletes a message. Required: thread_id, message_id. Returns the DeleteMessageResponse. Destructive."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread to which this message belongs.")),
			mcplib.WithString("message_id", mcplib.Required(), mcplib.Description("The ID of the message to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/threads/{thread_id}/messages/{message_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "message_id", WireName: "message_id", Location: "path"}}, []string{"thread_id", "message_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_messages_get",
			mcplib.WithDescription("Retrieve a message. Required: thread_id, message_id. Returns the MessageObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the [thread](/docs/api-reference/threads) to which this message belongs.")),
			mcplib.WithString("message_id", mcplib.Required(), mcplib.Description("The ID of the message to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/threads/{thread_id}/messages/{message_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "message_id", WireName: "message_id", Location: "path"}}, []string{"thread_id", "message_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_messages_list",
			mcplib.WithDescription("Returns a list of messages for a given thread. Required: thread_id. Optional: limit (default: 20), order (default: desc), after (plus 2 more)."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the [thread](/docs/api-reference/threads) the messages belong to.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("run_id", mcplib.Description("Filter messages by the run ID that generated them.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/threads/{thread_id}/messages", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "run_id", WireName: "run_id", Location: "query"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_messages_modify",
			mcplib.WithDescription("Modifies a message. Required: thread_id, message_id. Optional: metadata. Returns the new MessageObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread to which this message belongs.")),
			mcplib.WithString("message_id", mcplib.Required(), mcplib.Description("The ID of the message to modify.")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads/{thread_id}/messages/{message_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "message_id", WireName: "message_id", Location: "path"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}}, []string{"thread_id", "message_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_runs_cancel",
			mcplib.WithDescription("Cancels a run that is `in_progress`. Required: thread_id, run_id. Returns the new RunObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread to which this run belongs.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to cancel.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads/{thread_id}/runs/{run_id}/cancel", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}}, []string{"thread_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_runs_create",
			mcplib.WithDescription("Create a run. Required: thread_id, assistant_id. Optional: include[], additional_instructions, additional_messages (plus 14 more). Returns the new RunObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread to run.")),
			mcplib.WithString("include[]", mcplib.Description("A list of additional fields to include in the response.")),
			mcplib.WithString("additional_instructions", mcplib.Description("Appends additional instructions at the end of the instructions for the run. This is useful for modifying the...")),
			mcplib.WithString("additional_messages", mcplib.Description("Adds additional messages to the thread before creating the run.")),
			mcplib.WithString("assistant_id", mcplib.Required(), mcplib.Description("The ID of the [assistant](/docs/api-reference/assistants) to use to execute this run.")),
			mcplib.WithString("instructions", mcplib.Description("Overrides the [instructions](/docs/api-reference/assistants/createAssistant) of the assistant. This is useful for...")),
			mcplib.WithNumber("max_completion_tokens", mcplib.Description("The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort...")),
			mcplib.WithNumber("max_prompt_tokens", mcplib.Description("The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to...")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("model", mcplib.Description("The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it...")),
			mcplib.WithBoolean("parallel_tool_calls", mcplib.Description("Whether to enable [parallel function...")),
			mcplib.WithString("reasoning_effort", mcplib.Description("Reasoning effort")),
			mcplib.WithString("response_format", mcplib.Description("Specifies the format that the model must output. Compatible with [GPT-4o](/docs/models#gpt-4o), [GPT-4...")),
			mcplib.WithBoolean("stream", mcplib.Description("If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run...")),
			mcplib.WithNumber("temperature", mcplib.Description("What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while...")),
			mcplib.WithString("tool_choice", mcplib.Description("Tool choice")),
			mcplib.WithString("tools", mcplib.Description("Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.")),
			mcplib.WithNumber("top_p", mcplib.Description("An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the...")),
			mcplib.WithString("truncation_strategy", mcplib.Description("Controls for how a thread will be truncated prior to the run. Use this to control the initial context window of the run.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads/{thread_id}/runs", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "include[]", WireName: "include[]", Location: "query"}, {PublicName: "additional_instructions", WireName: "additional_instructions", Location: "body"}, {PublicName: "additional_messages", WireName: "additional_messages", Location: "body"}, {PublicName: "assistant_id", WireName: "assistant_id", Location: "body"}, {PublicName: "instructions", WireName: "instructions", Location: "body"}, {PublicName: "max_completion_tokens", WireName: "max_completion_tokens", Location: "body"}, {PublicName: "max_prompt_tokens", WireName: "max_prompt_tokens", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "parallel_tool_calls", WireName: "parallel_tool_calls", Location: "body"}, {PublicName: "reasoning_effort", WireName: "reasoning_effort", Location: "body"}, {PublicName: "response_format", WireName: "response_format", Location: "body"}, {PublicName: "stream", WireName: "stream", Location: "body"}, {PublicName: "temperature", WireName: "temperature", Location: "body"}, {PublicName: "tool_choice", WireName: "tool_choice", Location: "body"}, {PublicName: "tools", WireName: "tools", Location: "body"}, {PublicName: "top_p", WireName: "top_p", Location: "body"}, {PublicName: "truncation_strategy", WireName: "truncation_strategy", Location: "body"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_runs_get",
			mcplib.WithDescription("Retrieves a run. Required: thread_id, run_id. Returns the RunObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the [thread](/docs/api-reference/threads) that was run.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/threads/{thread_id}/runs/{run_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}}, []string{"thread_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_runs_get-step",
			mcplib.WithDescription("Retrieves a run step. Required: thread_id, run_id, step_id. Optional: include[]. Returns the RunStepObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread to which the run and run step belongs.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to which the run step belongs.")),
			mcplib.WithString("step_id", mcplib.Required(), mcplib.Description("The ID of the run step to retrieve.")),
			mcplib.WithString("include[]", mcplib.Description("A list of additional fields to include in the response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/threads/{thread_id}/runs/{run_id}/steps/{step_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}, {PublicName: "step_id", WireName: "step_id", Location: "path"}, {PublicName: "include[]", WireName: "include[]", Location: "query"}}, []string{"thread_id", "run_id", "step_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_runs_list",
			mcplib.WithDescription("Returns a list of runs belonging to a thread. Required: thread_id. Optional: limit (default: 20), order (default: desc), after (plus 1 more)."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread the run belongs to.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/threads/{thread_id}/runs", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}}, []string{"thread_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_runs_list-steps",
			mcplib.WithDescription("Returns a list of run steps belonging to a run. Required: thread_id, run_id. Optional: limit (default: 20), order (default: desc), after (plus 2 more)."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the thread the run and run steps belong to.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run the run steps belong to.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("include[]", mcplib.Description("A list of additional fields to include in the response.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/threads/{thread_id}/runs/{run_id}/steps", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "include[]", WireName: "include[]", Location: "query"}}, []string{"thread_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_runs_modify",
			mcplib.WithDescription("Modifies a run. Required: thread_id, run_id. Optional: metadata. Returns the new RunObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the [thread](/docs/api-reference/threads) that was run.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run to modify.")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads/{thread_id}/runs/{run_id}", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}}, []string{"thread_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("threads_runs_submit-tool-ouputs-to",
			mcplib.WithDescription("When a run has the `status: 'requires_action'` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. Required: thread_id, run_id, tool_outputs. Optional: stream. Returns the new RunObject."),
			mcplib.WithString("thread_id", mcplib.Required(), mcplib.Description("The ID of the [thread](/docs/api-reference/threads) to which this run belongs.")),
			mcplib.WithString("run_id", mcplib.Required(), mcplib.Description("The ID of the run that requires the tool output submission.")),
			mcplib.WithString("stream", mcplib.Description("Stream")),
			mcplib.WithString("tool_outputs", mcplib.Required(), mcplib.Description("A list of tools for which the outputs are being submitted.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/threads/{thread_id}/runs/{run_id}/submit_tool_outputs", []mcpParamBinding{{PublicName: "thread_id", WireName: "thread_id", Location: "path"}, {PublicName: "run_id", WireName: "run_id", Location: "path"}, {PublicName: "stream", WireName: "stream", Location: "body"}, {PublicName: "tool_outputs", WireName: "tool_outputs", Location: "body"}}, []string{"thread_id", "run_id"}),
	)
	s.AddTool(
		mcplib.NewTool("uploads_create",
			mcplib.WithDescription("Creates an intermediate [Upload](/docs/api-reference/uploads/object) object that you can add [Parts](/docs/api-reference/uploads/part-object) to. Currently, an Upload can accept at most 8 GB in total and expires after an hour after you create it. Once you complete the Upload, we will create a [File](/docs/api-reference/files/object) object that contains all the parts you uploaded. This File is usable in the rest of our platform as a regular File object. For certain `purpose` values, the correct `mime_type` must be specified. Please refer to documentation for the [supported MIME types for your use case](/docs/assistants/tools/file-search#supported-files). For guidance on the proper filename extensions for each purpose, please follow the documentation on [creating a File](/docs/api-reference/files/create). Returns the Upload object with status `pending`. Required: bytes, filename, mime_type, purpose. Optional: expires_after."),
			mcplib.WithNumber("bytes", mcplib.Required(), mcplib.Description("The number of bytes in the file you are uploading.")),
			mcplib.WithString("expires_after", mcplib.Description("The expiration policy for a file. By default, files with `purpose=batch` expire after 30 days and all other files...")),
			mcplib.WithString("filename", mcplib.Required(), mcplib.Description("The name of the file to upload.")),
			mcplib.WithString("mime_type", mcplib.Required(), mcplib.Description("The MIME type of the file. This must fall within the supported MIME types for your file purpose. See the supported...")),
			mcplib.WithString("purpose", mcplib.Required(), mcplib.Description("The intended purpose of the uploaded file. See the [documentation on File...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/uploads", []mcpParamBinding{{PublicName: "bytes", WireName: "bytes", Location: "body"}, {PublicName: "expires_after", WireName: "expires_after", Location: "body"}, {PublicName: "filename", WireName: "filename", Location: "body"}, {PublicName: "mime_type", WireName: "mime_type", Location: "body"}, {PublicName: "purpose", WireName: "purpose", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("uploads_cancel_upload",
			mcplib.WithDescription("Cancels the Upload. No Parts may be added after an Upload is cancelled. Returns the Upload object with status `cancelled`. Required: upload_id."),
			mcplib.WithString("upload_id", mcplib.Required(), mcplib.Description("The ID of the Upload.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/uploads/{upload_id}/cancel", []mcpParamBinding{{PublicName: "upload_id", WireName: "upload_id", Location: "path"}}, []string{"upload_id"}),
	)
	s.AddTool(
		mcplib.NewTool("uploads_complete_upload",
			mcplib.WithDescription("Completes the [Upload](/docs/api-reference/uploads/object). Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform. You can specify the order of the Parts by passing in an ordered list of the Part IDs. The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed. Returns the Upload object with status `completed`, including an additional `file` property containing the created usable File object. Required: upload_id, part_ids. Optional: md5."),
			mcplib.WithString("upload_id", mcplib.Required(), mcplib.Description("The ID of the Upload.")),
			mcplib.WithString("md5", mcplib.Description("The optional md5 checksum for the file contents to verify if the bytes uploaded matches what you expect.")),
			mcplib.WithString("part_ids", mcplib.Required(), mcplib.Description("The ordered list of Part IDs.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/uploads/{upload_id}/complete", []mcpParamBinding{{PublicName: "upload_id", WireName: "upload_id", Location: "path"}, {PublicName: "md5", WireName: "md5", Location: "body"}, {PublicName: "part_ids", WireName: "part_ids", Location: "body"}}, []string{"upload_id"}),
	)
	s.AddTool(
		mcplib.NewTool("uploads_parts_add-upload",
			mcplib.WithDescription("Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload. Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB. It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete). Required: upload_id, data. Returns the new UploadPart."),
			mcplib.WithString("upload_id", mcplib.Required(), mcplib.Description("The ID of the Upload.")),
			mcplib.WithString("data", mcplib.Required(), mcplib.Description("The chunk of bytes for this Part.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/uploads/{upload_id}/parts", []mcpParamBinding{{PublicName: "upload_id", WireName: "upload_id", Location: "path", RequestContentType: "multipart/form-data"}, {PublicName: "data", WireName: "data", Location: "body", Format: "binary", RequestContentType: "multipart/form-data"}}, []string{"upload_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_create",
			mcplib.WithDescription("Create a vector store. Optional: chunking_strategy, description, expires_after (plus 3 more). Returns the new VectorStoreObject."),
			mcplib.WithString("chunking_strategy", mcplib.Description("The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy. Only applicable if...")),
			mcplib.WithString("description", mcplib.Description("A description for the vector store. Can be used to describe the vector store's purpose.")),
			mcplib.WithString("expires_after", mcplib.Description("The expiration policy for a vector store.")),
			mcplib.WithString("file_ids", mcplib.Description("A list of [File](/docs/api-reference/files) IDs that the vector store should use. Useful for tools like...")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("name", mcplib.Description("The name of the vector store.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/vector_stores", []mcpParamBinding{{PublicName: "chunking_strategy", WireName: "chunking_strategy", Location: "body"}, {PublicName: "description", WireName: "description", Location: "body"}, {PublicName: "expires_after", WireName: "expires_after", Location: "body"}, {PublicName: "file_ids", WireName: "file_ids", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_delete",
			mcplib.WithDescription("Delete a vector store. Required: vector_store_id. Returns the DeleteVectorStoreResponse. Destructive."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/vector_stores/{vector_store_id}", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}}, []string{"vector_store_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_get",
			mcplib.WithDescription("Retrieves a vector store. Required: vector_store_id. Returns the VectorStoreObject."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vector_stores/{vector_store_id}", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}}, []string{"vector_store_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_list",
			mcplib.WithDescription("Returns a list of vector stores. Optional: limit (default: 20), order (default: desc), after (plus 1 more)."),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vector_stores", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_modify",
			mcplib.WithDescription("Modifies a vector store. Required: vector_store_id. Optional: expires_after, metadata, name. Returns the new VectorStoreObject."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store to modify.")),
			mcplib.WithString("expires_after", mcplib.Description("The expiration policy for a vector store.")),
			mcplib.WithString("metadata", mcplib.Description("Metadata")),
			mcplib.WithString("name", mcplib.Description("The name of the vector store.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/vector_stores/{vector_store_id}", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "expires_after", WireName: "expires_after", Location: "body"}, {PublicName: "metadata", WireName: "metadata", Location: "body"}, {PublicName: "name", WireName: "name", Location: "body"}}, []string{"vector_store_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_file-batches_cancel-vector-store-file-batch",
			mcplib.WithDescription("Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible. Required: vector_store_id, batch_id. Returns the new VectorStoreFileBatchObject."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store that the file batch belongs to.")),
			mcplib.WithString("batch_id", mcplib.Required(), mcplib.Description("The ID of the file batch to cancel.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/vector_stores/{vector_store_id}/file_batches/{batch_id}/cancel", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "batch_id", WireName: "batch_id", Location: "path"}}, []string{"vector_store_id", "batch_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_file-batches_create-vector-store-file-batch",
			mcplib.WithDescription("The maximum number of files in a single batch request is 2000. Vector store file attach requests are rate limited per vector store (300 requests per minute across both this endpoint and `/vector_stores/{vector_store_id}/files`). For ingesting multiple files into the same vector store, this batch endpoint is recommended. Required: vector_store_id. Returns the new VectorStoreFileBatchObject."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store for which to create a File Batch.")),
			mcplib.WithString("body_json", mcplib.Required(), mcplib.Description("Request body as a JSON object string (this endpoint accepts a polymorphic schema: oneOf/anyOf)")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/vector_stores/{vector_store_id}/file_batches", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "body_json", WireName: "body_json", Location: "body_json"}}, []string{"vector_store_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_file-batches_get-vector-store-file-batch",
			mcplib.WithDescription("Retrieves a vector store file batch. Required: vector_store_id, batch_id. Returns the VectorStoreFileBatchObject."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store that the file batch belongs to.")),
			mcplib.WithString("batch_id", mcplib.Required(), mcplib.Description("The ID of the file batch being retrieved.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vector_stores/{vector_store_id}/file_batches/{batch_id}", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "batch_id", WireName: "batch_id", Location: "path"}}, []string{"vector_store_id", "batch_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_file-batches_list-files-in-vector-store-batch",
			mcplib.WithDescription("Returns a list of vector store files in a batch. Required: vector_store_id, batch_id. Optional: limit (default: 20), order (default: desc), after (plus 2 more)."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store that the files belong to.")),
			mcplib.WithString("batch_id", mcplib.Required(), mcplib.Description("The ID of the file batch that the files belong to.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("filter", mcplib.Description("Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vector_stores/{vector_store_id}/file_batches/{batch_id}/files", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "batch_id", WireName: "batch_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "filter", WireName: "filter", Location: "query"}}, []string{"vector_store_id", "batch_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_files_create-vector-store",
			mcplib.WithDescription("This endpoint is subject to a per-vector-store write rate limit of 300 requests per minute, shared with `/vector_stores/{vector_store_id}/file_batches`. For uploading multiple files to the same vector store, use the file batches endpoint to reduce request volume. Required: vector_store_id, file_id. Optional: attributes, chunking_strategy. Returns the new VectorStoreFileObject."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store for which to create a File.")),
			mcplib.WithString("attributes", mcplib.Description("Attributes")),
			mcplib.WithString("chunking_strategy", mcplib.Description("The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy.")),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("A [File](/docs/api-reference/files) ID that the vector store should use. Useful for tools like `file_search` that...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/vector_stores/{vector_store_id}/files", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "attributes", WireName: "attributes", Location: "body"}, {PublicName: "chunking_strategy", WireName: "chunking_strategy", Location: "body"}, {PublicName: "file_id", WireName: "file_id", Location: "body"}}, []string{"vector_store_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_files_delete-vector-store",
			mcplib.WithDescription("Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint. Required: vector_store_id, file_id. Returns the DeleteVectorStoreFileResponse. Destructive."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store that the file belongs to.")),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("The ID of the file to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/vector_stores/{vector_store_id}/files/{file_id}", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"vector_store_id", "file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_files_get-vector-store",
			mcplib.WithDescription("Retrieves a vector store file. Required: vector_store_id, file_id. Returns the VectorStoreFileObject."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store that the file belongs to.")),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("The ID of the file being retrieved.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vector_stores/{vector_store_id}/files/{file_id}", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"vector_store_id", "file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_files_list-vector-store",
			mcplib.WithDescription("Returns a list of vector store files. Required: vector_store_id. Optional: limit (default: 20), order (default: desc), after (plus 2 more)."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store that the files belong to.")),
			mcplib.WithNumber("limit", mcplib.Description("A limit on the number of objects to be returned.")),
			mcplib.WithString("order", mcplib.Description("Sort order by the `created_at` timestamp of the objects.")),
			mcplib.WithString("after", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("before", mcplib.Description("A cursor for use in pagination.")),
			mcplib.WithString("filter", mcplib.Description("Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vector_stores/{vector_store_id}/files", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}, {PublicName: "before", WireName: "before", Location: "query"}, {PublicName: "filter", WireName: "filter", Location: "query"}}, []string{"vector_store_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_files_retrieve-vector-store-content",
			mcplib.WithDescription("Retrieve the parsed contents of a vector store file. Required: vector_store_id, file_id. Returns array of FilesRetrieveVectorStoreContentItem."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store.")),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("The ID of the file within the vector store.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/vector_stores/{vector_store_id}/files/{file_id}/content", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "file_id", WireName: "file_id", Location: "path"}}, []string{"vector_store_id", "file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_files_update-vector-store-attributes",
			mcplib.WithDescription("Update attributes on a vector store file. Required: vector_store_id, file_id, attributes. Returns the new VectorStoreFileObject."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store the file belongs to.")),
			mcplib.WithString("file_id", mcplib.Required(), mcplib.Description("The ID of the file to update attributes.")),
			mcplib.WithString("attributes", mcplib.Required(), mcplib.Description("Attributes")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/vector_stores/{vector_store_id}/files/{file_id}", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "file_id", WireName: "file_id", Location: "path"}, {PublicName: "attributes", WireName: "attributes", Location: "body"}}, []string{"vector_store_id", "file_id"}),
	)
	s.AddTool(
		mcplib.NewTool("vector-stores_search_vector-store",
			mcplib.WithDescription("Search a vector store for relevant chunks based on a query and file attributes filter. Required: vector_store_id, query. Optional: filters, max_num_results (default: 10), ranking_options (plus 1 more). Returns array of VectorStoreSearchResultItem."),
			mcplib.WithString("vector_store_id", mcplib.Required(), mcplib.Description("The ID of the vector store to search.")),
			mcplib.WithString("filters", mcplib.Description("A filter to apply based on file attributes.")),
			mcplib.WithNumber("max_num_results", mcplib.Description("The maximum number of results to return. This number should be between 1 and 50 inclusive.")),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("A query string for a search")),
			mcplib.WithString("ranking_options", mcplib.Description("Ranking options for search.")),
			mcplib.WithBoolean("rewrite_query", mcplib.Description("Whether to rewrite the natural language query for vector search.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/vector_stores/{vector_store_id}/search", []mcpParamBinding{{PublicName: "vector_store_id", WireName: "vector_store_id", Location: "path"}, {PublicName: "filters", WireName: "filters", Location: "body"}, {PublicName: "max_num_results", WireName: "max_num_results", Location: "body"}, {PublicName: "query", WireName: "query", Location: "body"}, {PublicName: "ranking_options", WireName: "ranking_options", Location: "body"}, {PublicName: "rewrite_query", WireName: "rewrite_query", Location: "body"}}, []string{"vector_store_id"}),
	)
	s.AddTool(
		mcplib.NewTool("videos_create",
			mcplib.WithDescription("Create a new video generation job from a prompt and optional reference assets. Required: prompt. Optional: input_reference, model, seconds (plus 1 more). Returns the new VideoResource."),
			mcplib.WithString("input_reference", mcplib.Description("Optional reference object that guides generation. Provide exactly one of `image_url` or `file_id`.")),
			mcplib.WithString("model", mcplib.Description("The video generation model to use (allowed values: sora-2, sora-2-pro). Defaults to `sora-2`.")),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("Text prompt that describes the video to generate.")),
			mcplib.WithString("seconds", mcplib.Description("Clip duration in seconds (allowed values: 4, 8, 12). Defaults to 4 seconds.")),
			mcplib.WithString("size", mcplib.Description("Output resolution formatted as width x height (allowed values: 720x1280, 1280x720, 1024x1792, 1792x1024). Defaults...")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/videos", []mcpParamBinding{{PublicName: "input_reference", WireName: "input_reference", Location: "body"}, {PublicName: "model", WireName: "model", Location: "body"}, {PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "seconds", WireName: "seconds", Location: "body"}, {PublicName: "size", WireName: "size", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("videos_create-character",
			mcplib.WithDescription("Create a character from an uploaded video. Required: name, video. Returns the new VideoCharacterResource."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Display name for this API character.")),
			mcplib.WithString("video", mcplib.Required(), mcplib.Description("Video file used to create a character.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/videos/characters", []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "body", RequestContentType: "multipart/form-data"}, {PublicName: "video", WireName: "video", Location: "body", Format: "binary", RequestContentType: "multipart/form-data"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("videos_create-edit",
			mcplib.WithDescription("Create a new video generation job by editing a source video or existing generated video. Required: prompt, video. Returns the new VideoResource."),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("Text prompt that describes how to edit the source video.")),
			mcplib.WithString("video", mcplib.Required(), mcplib.Description("Reference to the completed video to edit.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/videos/edits", []mcpParamBinding{{PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "video", WireName: "video", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("videos_create-extend",
			mcplib.WithDescription("Create an extension of a completed video. Required: prompt, seconds, video. Returns the new VideoResource."),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("Updated text prompt that directs the extension generation.")),
			mcplib.WithString("seconds", mcplib.Required(), mcplib.Description("Length of the newly generated extension segment in seconds (allowed values: 4, 8, 12, 16, 20).")),
			mcplib.WithString("video", mcplib.Required(), mcplib.Description("Reference to the completed video to extend.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/videos/extensions", []mcpParamBinding{{PublicName: "prompt", WireName: "prompt", Location: "body"}, {PublicName: "seconds", WireName: "seconds", Location: "body"}, {PublicName: "video", WireName: "video", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("videos_delete",
			mcplib.WithDescription("Permanently delete a completed or failed video and its stored assets. Required: video_id. Returns the DeletedVideoResource. Destructive."),
			mcplib.WithString("video_id", mcplib.Required(), mcplib.Description("The identifier of the video to delete.")),
			mcplib.WithDestructiveHintAnnotation(true),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("DELETE", "/videos/{video_id}", []mcpParamBinding{{PublicName: "video_id", WireName: "video_id", Location: "path"}}, []string{"video_id"}),
	)
	s.AddTool(
		mcplib.NewTool("videos_get",
			mcplib.WithDescription("Fetch the latest metadata for a generated video. Required: video_id. Returns the VideoResource."),
			mcplib.WithString("video_id", mcplib.Required(), mcplib.Description("The identifier of the video to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/videos/{video_id}", []mcpParamBinding{{PublicName: "video_id", WireName: "video_id", Location: "path"}}, []string{"video_id"}),
	)
	s.AddTool(
		mcplib.NewTool("videos_get-character",
			mcplib.WithDescription("Fetch a character. Required: character_id. Returns the VideoCharacterResource."),
			mcplib.WithString("character_id", mcplib.Required(), mcplib.Description("The identifier of the character to retrieve.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/videos/characters/{character_id}", []mcpParamBinding{{PublicName: "character_id", WireName: "character_id", Location: "path"}}, []string{"character_id"}),
	)
	s.AddTool(
		mcplib.NewTool("videos_list",
			mcplib.WithDescription("List recently generated videos for the current project. Optional: limit, order, after. Returns array of VideoResource."),
			mcplib.WithNumber("limit", mcplib.Description("Number of items to retrieve")),
			mcplib.WithString("order", mcplib.Description("Sort order of results by timestamp. Use `asc` for ascending order or `desc` for descending order.")),
			mcplib.WithString("after", mcplib.Description("Identifier for the last item from the previous pagination request")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/videos", []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "order", WireName: "order", Location: "query"}, {PublicName: "after", WireName: "after", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("videos_content_retrieve-video",
			mcplib.WithDescription("Download the generated video bytes or a derived preview asset. Streams the rendered video content for the specified video job. Required: video_id. Optional: variant."),
			mcplib.WithString("video_id", mcplib.Required(), mcplib.Description("The identifier of the video whose media to download.")),
			mcplib.WithString("variant", mcplib.Description("Which downloadable asset to return. Defaults to the MP4 video.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/videos/{video_id}/content", []mcpParamBinding{{PublicName: "video_id", WireName: "video_id", Location: "path"}, {PublicName: "variant", WireName: "variant", Location: "query"}}, []string{"video_id"}),
	)
	s.AddTool(
		mcplib.NewTool("videos_remix_create-video",
			mcplib.WithDescription("Create a remix of a completed video using a refreshed prompt. Required: video_id, prompt. Returns the new VideoResource."),
			mcplib.WithString("video_id", mcplib.Required(), mcplib.Description("The identifier of the completed video to remix.")),
			mcplib.WithString("prompt", mcplib.Required(), mcplib.Description("Updated text prompt that directs the remix generation.")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/videos/{video_id}/remix", []mcpParamBinding{{PublicName: "video_id", WireName: "video_id", Location: "path"}, {PublicName: "prompt", WireName: "prompt", Location: "body"}}, []string{"video_id"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName         string
	WireName           string
	Location           string
	Format             string
	RequestContentType string
}

func mcpMultipartFieldValue(v any) string {
	if s, ok := v.(string); ok {
		return s
	}
	if data, err := json.Marshal(v); err == nil {
		return string(data)
	}
	return fmt.Sprintf("%v", v)
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		multipartFields := make(map[string]string)
		multipartFileFields := make(map[string]string)
		multipart := false
		// bodyJSONOverride holds an opaque body for endpoints whose request
		// schema is a oneOf/anyOf and was wired with a single body_json input.
		// When set, it replaces the bodyArgs map for POST/PUT/PATCH.
		var bodyJSONOverride json.RawMessage
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			if strings.EqualFold(binding.RequestContentType, "multipart/form-data") {
				multipart = true
			}
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
				if multipart {
					if strings.EqualFold(binding.Format, "binary") {
						multipartFileFields[binding.WireName] = fmt.Sprintf("%v", v)
					} else {
						multipartFields[binding.WireName] = mcpMultipartFieldValue(v)
					}
				}
			case "body_json":
				if s, ok := v.(string); ok && s != "" {
					var parsed any
					if err := json.Unmarshal([]byte(s), &parsed); err != nil {
						return mcplib.NewToolResultError("body_json must be a valid JSON object: " + err.Error()), nil
					}
					if _, isMap := parsed.(map[string]any); !isMap {
						return mcplib.NewToolResultError(fmt.Sprintf("body_json must be a JSON object, got JSON %T", parsed)), nil
					}
					bodyJSONOverride = json.RawMessage(s)
				}
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
				if multipart {
					multipartFields[k] = mcpMultipartFieldValue(v)
				}
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			if multipart {
				data, _, err = c.PostMultipartWithParams(path, params, multipartFields, multipartFileFields)
				break
			}
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.PostWithParams(path, params, bodyJSONOverride)
				break
			}
			data, _, err = c.PostWithParams(path, params, bodyArgs)
		case "PUT":
			if multipart {
				data, _, err = c.PutMultipartWithParams(path, params, multipartFields, multipartFileFields)
				break
			}
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.PutWithParams(path, params, bodyJSONOverride)
				break
			}
			data, _, err = c.PutWithParams(path, params, bodyArgs)
		case "PATCH":
			if multipart {
				data, _, err = c.PatchMultipartWithParams(path, params, multipartFields, multipartFileFields)
				break
			}
			if len(bodyJSONOverride) > 0 {
				data, _, err = c.PatchWithParams(path, params, bodyJSONOverride)
				break
			}
			data, _, err = c.PatchWithParams(path, params, bodyArgs)
		case "DELETE":
			data, _, err = c.DeleteWithParams(path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export OPENAI_API_KEY_AUTH=<your-key>" +
					"\n      See API docs: https://help.openai.com/" +
					"\n      Run 'openai-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export OPENAI_API_KEY_AUTH=<your-key>" +
					"\n      See API docs: https://help.openai.com/" +
					"\n      Run 'openai-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export OPENAI_API_KEY_AUTH=<your-key>" +
					"\n      See API docs: https://help.openai.com/" +
					"\n      Run 'openai-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "openai-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "openai-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "openai",
		"description": "The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.",
		"archetype":   "communication",
		"tool_count":  242,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion openai-pp-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "OPENAI_API_KEY_AUTH",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"docs_url": "https://help.openai.com/",
		},
		"resources": []map[string]any{
			{
				"name":        "assistants",
				"description": "Build Assistants that can call models and use tools.",
				"endpoints":   []string{"create", "delete", "get", "list", "modify"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "audio",
				"description": "Turn audio into text or text into audio.",
				"endpoints":   []string{"create-speech", "create-transcription", "create-translation", "create-voice", "create-voice-consent", "delete-voice-consent", "get-voice-consent", "list-voice-consents", "update-voice-consent"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "batches",
				"description": "Manage batches",
				"endpoints":   []string{"create-batch", "list", "retrieve-batch"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "chat",
				"description": "Given a list of messages comprising a conversation, the model will return a response.",
				"endpoints":   []string{"create-completion", "delete-completion", "get-completion", "get-completion-messages", "list-completions", "update-completion"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "chatkit",
				"description": "Manage chatkit",
				"endpoints":   []string{"cancel-chat-session-method", "create-chat-session-method", "delete-thread-method", "get-thread-method", "list-thread-items-method", "list-threads-method"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "completions",
				"description": "Given a prompt, the model will return one or more predicted completions, and can also return the probabilities of...",
				"endpoints":   []string{"create"},
				"searchable":  true,
			},
			{
				"name":        "containers",
				"description": "Manage containers",
				"endpoints":   []string{"create", "delete", "list", "retrieve"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "conversations",
				"description": "Manage conversations and conversation items.",
				"endpoints":   []string{"create", "delete", "get", "update"},
				"searchable":  true,
			},
			{
				"name":        "embeddings",
				"description": "Get a vector representation of a given input that can be easily consumed by machine learning models and algorithms.",
				"endpoints":   []string{"create"},
				"searchable":  true,
			},
			{
				"name":        "evals",
				"description": "Manage and run evals in the OpenAI platform.",
				"endpoints":   []string{"create", "delete", "get", "list", "update"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "files",
				"description": "Files are used to upload documents that can be used with features like Assistants and Fine-tuning.",
				"endpoints":   []string{"create", "delete", "list", "retrieve"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "fine-tuning",
				"description": "Manage fine-tuning jobs to tailor a model to your specific training data.",
				"endpoints":   []string{"cancel-job", "create-checkpoint-permission", "create-job", "delete-checkpoint-permission", "list-checkpoint-permissions", "list-events", "list-job-checkpoints", "list-paginated-jobs", "pause-job", "resume-job", "retrieve-job", "run-grader", "validate-grader"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "images",
				"description": "Given a prompt and/or an input image, the model will generate a new image.",
				"endpoints":   []string{"create", "create-edit", "create-variation"},
				"searchable":  true,
			},
			{
				"name":        "models",
				"description": "List and describe the various models available in the API.",
				"endpoints":   []string{"delete", "list", "retrieve"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "moderations",
				"description": "Given text and/or image inputs, classifies if those inputs are potentially harmful.",
				"endpoints":   []string{"create"},
				"searchable":  true,
			},
			{
				"name":        "organization",
				"description": "Manage organization",
				"endpoints":   []string{"activate-certificates", "activate-project-certificates", "add-group-user", "add-project-group", "admin-api-keys-create", "admin-api-keys-delete", "admin-api-keys-get", "admin-api-keys-list", "archive-project", "assign-group-role", "assign-user-role", "create-group", "create-project", "create-project-service-account", "create-project-user", "create-role", "deactivate-certificates", "deactivate-project-certificates", "delete-certificate", "delete-group", "delete-invite", "delete-project-api-key", "delete-project-service-account", "delete-project-user", "delete-role", "delete-user", "get-certificate", "invite-user", "list-audit-logs", "list-certificates", "list-group-role-assignments", "list-group-users", "list-groups", "list-invites", "list-project-api-keys", "list-project-certificates", "list-project-groups", "list-project-rate-limits", "list-project-service-accounts", "list-project-users", "list-projects", "list-roles", "list-user-role-assignments", "list-users", "modify-certificate", "modify-project", "modify-project-user", "modify-user", "remove-group-user", "remove-project-group", "retrieve-invite", "retrieve-project", "retrieve-project-api-key", "retrieve-project-service-account", "retrieve-project-user", "retrieve-user", "unassign-group-role", "unassign-user-role", "update-group", "update-project-rate-limits", "update-role", "upload-certificate", "usage-audio-speeches", "usage-audio-transcriptions", "usage-code-interpreter-sessions", "usage-completions", "usage-costs", "usage-embeddings", "usage-images", "usage-moderations", "usage-vector-stores"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "projects",
				"description": "Manage projects",
				"endpoints":   []string{},
			},
			{
				"name":        "realtime",
				"description": "Manage realtime",
				"endpoints":   []string{"accept-call", "create-call", "create-client-secret", "create-session", "create-transcription-session", "create-translation-client-secret", "hangup-call", "refer-call", "reject-call"},
				"searchable":  true,
			},
			{
				"name":        "responses",
				"description": "Manage responses",
				"endpoints":   []string{"compactconversation", "create", "delete", "get", "getinputtokencounts"},
				"searchable":  true,
			},
			{
				"name":        "skills",
				"description": "Manage skills",
				"endpoints":   []string{"create", "delete", "get", "list", "update-default-version"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "threads",
				"description": "Manage threads",
				"endpoints":   []string{"create", "create-and-run", "delete", "get", "modify"},
				"searchable":  true,
			},
			{
				"name":        "uploads",
				"description": "Use Uploads to upload large files in multiple parts.",
				"endpoints":   []string{"create"},
				"searchable":  true,
			},
			{
				"name":        "vector-stores",
				"description": "Manage vector stores",
				"endpoints":   []string{"create", "delete", "get", "list", "modify"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "videos",
				"description": "Manage videos",
				"endpoints":   []string{"create", "create-character", "create-edit", "create-extend", "delete", "get", "get-character", "list"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		"playbook": []map[string]string{
			{"topic": "Message search", "insight": "Use the search tool on synced data rather than paginating through message history. Message APIs often have aggressive rate limits."},
			{"topic": "Channel health", "insight": "When analyzing channel activity, use the channel-health command or sql aggregation on synced messages. Don't iterate individual messages via API."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
